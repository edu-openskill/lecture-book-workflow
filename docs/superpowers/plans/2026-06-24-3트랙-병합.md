# 3-트랙 집필 하네스 병합 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `book-workflow` 하네스에 유튜브(lecture) 트랙을 병합해, 시작하자마자 `lecture`/`code`/`concept` 3-트랙 중 하나를 고르고 분기하도록 만든다.

**Architecture:** 현재 `book-workflow`를 베이스로 두고, 이미 존재하는 `track` 변수(`code`/`concept`)를 3값으로 확장한다. `lecture-book-workflow`에서 `lecture`·`lecture-caption` 스킬과 `step2-강의해부`·`step3-챕터매핑` 가이드를 **추가만** 하고, 공유 파일(CLAUDE.md, step1/4/5, CATALOG)에 lecture 분기를 끼워 넣는다. 어떤 기존 트랙 능력도 제거하지 않는다.

**Tech Stack:** Markdown 하네스 파일, JSON(progress), Python(yt-dlp 래퍼 스크립트 1개 — 복사만). 빌드/테스트는 "파일 존재 + 분기 일관성 + 참조 무결성" 검증으로 대체(이 하네스는 단위테스트 대상 코드가 아니라 프롬프트/워크플로우 자산).

## Global Constraints

- 대상 저장소: `C:\Users\ssarm\Documents\books\book-workflow` (git repo).
- 작업 브랜치: `feature/3-track-merge` (현재 `feature/개념트랙-이미지자동화`에서 분기). 기존 브랜치 직접 수정 금지.
- 소스 저장소(읽기 전용): `C:\Users\ssarm\AppData\Local\Temp\claude\C--Users-ssarm-Documents-books\be408759-2c02-4316-8198-f0abf1d59385\scratchpad\lecture-repo` (이하 `$LR`). 수정 금지.
- **추가만, 삭제 없음.** `code`/`math`/`image-gen`/`pdf`/`pdf-ty` 스킬과 `step2-코드해부.md`/`step3-시나리오.md`는 손대지 않는다.
- 트랙 내부 식별자는 정확히 `lecture` / `code` / `concept` (영문 소문자). 사용자 표시 라벨은 "유튜브 강의" / "소스코드" / "목차·주제".
- lecture-repo의 `projects/` 샘플 산출물은 가져오지 않는다.
- 모든 신규/수정 파일은 UTF-8, Markdown. 코드블록 언어 태그 필수.
- 각 태스크 끝에서 커밋. 커밋 메시지 말미에 `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`.

---

## File Structure

| 파일 | 책임 | 작업 |
|------|------|------|
| `.claude/skills/lecture/` | 강의 해부(개념·비유·선수·오개념 추출) | 복사(신규) |
| `.claude/skills/lecture-caption/` | 유튜브 자막 다운로드·정제·정렬 | 복사(신규) |
| `workflow/step2-강의해부.md` | lecture 트랙 STEP 2 가이드 | 복사(신규) |
| `workflow/step3-챕터매핑.md` | lecture 트랙 STEP 3 가이드 | 복사(신규) |
| `.claude/skills/CATALOG.md` | 스킬 카탈로그 | lecture 2개 등재 |
| `CLAUDE.md` | 핵심 정책 + 라우팅 + 상수 | 3-트랙 분기/라우팅/상수 |
| `workflow/step1-씨앗.md` | STEP 1 (공유) | lecture 트랙 분기 추가 |
| `workflow/step4-뼈대.md` | STEP 4 (공유) | lecture 자료 배치 분기 추가 |
| `workflow/step5-챕터집필.md` | STEP 5 (공유) | lecture 기술 파트 분기 추가 |
| `README.md` | 저장소 소개 | 3-트랙 소개로 갱신 |

---

## Task 1: 작업 브랜치 생성 + lecture 스킬 복사

**Files:**
- Create: `.claude/skills/lecture/` (전체), `.claude/skills/lecture-caption/` (전체)

**Interfaces:**
- Produces: `.claude/skills/lecture/SKILL.md` (name: `lecture`), `.claude/skills/lecture-caption/SKILL.md` (name: `lecture-caption`, scripts/fetch_captions.py, references/caption-cleanup.md). 이후 STEP 2 가이드와 CLAUDE.md/CATALOG가 이 스킬 이름을 참조한다.

- [ ] **Step 1: 작업 브랜치 생성**

Run:
```bash
cd /c/Users/ssarm/Documents/books/book-workflow && git checkout -b feature/3-track-merge
```
Expected: `Switched to a new branch 'feature/3-track-merge'`

- [ ] **Step 2: lecture / lecture-caption 스킬 디렉토리 복사**

Run:
```bash
LR="/c/Users/ssarm/AppData/Local/Temp/claude/C--Users-ssarm-Documents-books/be408759-2c02-4316-8198-f0abf1d59385/scratchpad/lecture-repo"
DST="/c/Users/ssarm/Documents/books/book-workflow/.claude/skills"
cp -r "$LR/.claude/skills/lecture" "$DST/lecture"
cp -r "$LR/.claude/skills/lecture-caption" "$DST/lecture-caption"
```

- [ ] **Step 3: 복사 검증 (파일 존재 + frontmatter name)**

Run:
```bash
DST="/c/Users/ssarm/Documents/books/book-workflow/.claude/skills"
ls "$DST/lecture/SKILL.md" "$DST/lecture/references/lecture-analysis.md" \
   "$DST/lecture-caption/SKILL.md" "$DST/lecture-caption/scripts/fetch_captions.py" \
   "$DST/lecture-caption/references/caption-cleanup.md"
grep -h "^name:" "$DST/lecture/SKILL.md" "$DST/lecture-caption/SKILL.md"
```
Expected: 5개 파일 모두 경로 출력 + `name: lecture`, `name: lecture-caption` 2줄.

- [ ] **Step 4: 하드코딩 경로 없음 확인**

Run:
```bash
DST="/c/Users/ssarm/Documents/books/book-workflow/.claude/skills"
grep -rn "lecture-repo\|scratchpad\|C--Users" "$DST/lecture" "$DST/lecture-caption" || echo "CLEAN"
```
Expected: `CLEAN` (소스 저장소 경로가 하드코딩돼 있지 않음).

- [ ] **Step 5: Commit**

```bash
cd /c/Users/ssarm/Documents/books/book-workflow
git add .claude/skills/lecture .claude/skills/lecture-caption
git commit -m "feat(lecture): lecture·lecture-caption 스킬 병합

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 2: lecture 트랙 워크플로우 가이드 복사 (step2·step3)

**Files:**
- Create: `workflow/step2-강의해부.md`, `workflow/step3-챕터매핑.md`

**Interfaces:**
- Consumes: Task 1의 `lecture` 스킬 (step2-강의해부가 로드).
- Produces: lecture 트랙 STEP 2 산출물 `planning/lecture-analysis.md`, STEP 3 산출물 `planning/chapter-map.md`. CLAUDE.md 라우팅(Task 4)이 이 파일명을 참조한다.

- [ ] **Step 1: 두 가이드 파일 복사**

Run:
```bash
LR="/c/Users/ssarm/AppData/Local/Temp/claude/C--Users-ssarm-Documents-books/be408759-2c02-4316-8198-f0abf1d59385/scratchpad/lecture-repo"
DST="/c/Users/ssarm/Documents/books/book-workflow/workflow"
cp "$LR/workflow/step2-강의해부.md" "$DST/step2-강의해부.md"
cp "$LR/workflow/step3-챕터매핑.md" "$DST/step3-챕터매핑.md"
```

- [ ] **Step 2: 기존 코드 트랙 가이드가 그대로 있는지(삭제 없음) 확인**

Run:
```bash
DST="/c/Users/ssarm/Documents/books/book-workflow/workflow"
ls "$DST/step2-코드해부.md" "$DST/step3-시나리오.md" "$DST/step2-강의해부.md" "$DST/step3-챕터매핑.md"
```
Expected: 4개 파일 모두 존재 (코드 트랙 2개 + lecture 트랙 2개 공존).

- [ ] **Step 3: 참조 무결성 확인 (가이드가 가리키는 스킬·경로가 실재)**

Run:
```bash
DST="/c/Users/ssarm/Documents/books/book-workflow"
grep -n "lecture\b\|sources/\|lecture-analysis\|chapter-map" "$DST/workflow/step2-강의해부.md" "$DST/workflow/step3-챕터매핑.md" | head -30
test -d "$DST/.claude/skills/lecture" && echo "lecture skill OK"
```
Expected: `sources/`, `lecture-analysis`, `chapter-map`, `lecture` 참조가 보이고 `lecture skill OK` 출력. (lecture 스킬은 Task 1에서 이미 추가됨.)

- [ ] **Step 4: Commit**

```bash
cd /c/Users/ssarm/Documents/books/book-workflow
git add workflow/step2-강의해부.md workflow/step3-챕터매핑.md
git commit -m "feat(lecture): STEP2 강의해부·STEP3 챕터매핑 가이드 병합

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 3: CATALOG.md에 lecture 스킬 2개 등재

**Files:**
- Modify: `.claude/skills/CATALOG.md`

**Interfaces:**
- Consumes: Task 1의 스킬 이름 `lecture`, `lecture-caption`.
- Produces: 없음(문서). CLAUDE.md가 "전체 스킬 카탈로그: CATALOG.md"로 가리킨다.

- [ ] **Step 1: 현재 CATALOG.md 구조 확인**

Run:
```bash
sed -n '1,30p' /c/Users/ssarm/Documents/books/book-workflow/.claude/skills/CATALOG.md
```
Expected: 상단에 "스킬 폴더 구조" 표(`| 폴더 | 모델 | 담당 스킬 | 참조 파일 |` 형식)가 보인다. 정확한 헤더 문구를 확인해 다음 Step의 삽입 위치를 잡는다.

- [ ] **Step 2: 스킬 폴더 표에 lecture 2행 추가**

`.claude/skills/CATALOG.md`의 "스킬 폴더 구조" 표에서, 마지막 데이터 행 다음(또는 첫 행 위)에 아래 2행을 추가한다. 컬럼 개수는 기존 표 헤더에 맞춰 정렬한다(헤더가 `| 폴더 | 모델 | 담당 스킬 | 참조 파일 |`이면 4컬럼):

```markdown
| `lecture/` ★lecture | claude-sonnet-4-6 | L1 개념 / L2 비유 / L3 선수 / L4 오개념 (강의 해부) | `references/lecture-analysis.md` |
| `lecture-caption/` ★lecture | claude-sonnet-4-6 | Cap1~Cap4 유튜브 자막 다운로드·정제·정렬 | `scripts/fetch_captions.py`, `references/caption-cleanup.md` |
```

- [ ] **Step 3: STEP↔스킬 매핑 섹션에 lecture 행 추가**

CATALOG.md에서 STEP별 스킬 매핑을 나열하는 표/목록을 찾아, lecture 트랙 진입점을 아래처럼 추가한다(기존 표 형식에 맞춰):

```markdown
| (사전) 자막 추출 | lecture-caption (Cap1~Cap4) — lecture 트랙 |
| 2. 강의 해부 (lecture) | lecture (L1 개념 / L2 비유 / L3 선수 / L4 오개념) |
| 3. 챕터 매핑 (lecture) | planning(분량) + lecture(L3 의존성) |
```

- [ ] **Step 4: 검증 — lecture 스킬이 카탈로그에 등재됨**

Run:
```bash
grep -c "lecture" /c/Users/ssarm/Documents/books/book-workflow/.claude/skills/CATALOG.md
```
Expected: 5 이상 (lecture/lecture-caption 폴더행 + 매핑행).

- [ ] **Step 5: Commit**

```bash
cd /c/Users/ssarm/Documents/books/book-workflow
git add .claude/skills/CATALOG.md
git commit -m "docs(catalog): lecture·lecture-caption 스킬 등재

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 4: CLAUDE.md — 3-트랙 분기(시작하자마자) + 라우팅 + 상수

이 태스크가 병합의 핵심이다. 트랙 질문을 **맨 앞·3지선다**로 만들고, lecture 라우팅과 상수를 추가한다.

**Files:**
- Modify: `CLAUDE.md`

**Interfaces:**
- Consumes: Task 1·2의 스킬/가이드 이름 (`lecture`, `lecture-caption`, `step2-강의해부.md`, `step3-챕터매핑.md`).
- Produces: 3-트랙 라우팅 정책. step1/4/5(Task 5~7)는 `progress.json.track` 값(`lecture`/`code`/`concept`)을 읽어 분기한다.

- [ ] **Step 1: 트랙 선택을 맨 앞·3지선다로 변경**

`CLAUDE.md`의 `### 새 책 만들기` 섹션에서, 현재 "책 이름 다음으로 트랙을 묻는다"로 된 **트랙 질문**(2지선다, code/concept) 항목을 찾는다. 아래 내용으로 교체한다 — 트랙 질문이 **책 이름보다 먼저** 오도록 순서를 바꾼다:

```markdown
- **STEP 0 — 트랙 선택(가장 먼저)**: 책 이름을 묻기 전에, 어떤 책을 쓸지 먼저 묻는다.
  > "어떤 방식으로 책을 쓸까요?
  >  1. **유튜브 강의로** — 녹화된 강의(판서대본 + 유튜브 자막)를 원고 소스로 (track=`lecture`)
  >  2. **소스코드로** — 완성된 코드를 분석해 책으로 (track=`code`)
  >  3. **목차/주제로** — 완성물 없이 목차·주제만으로 (track=`concept`)"
  - 답에 따라 `track`을 `lecture`/`code`/`concept` 중 하나로 확정한다.
- **그 다음 책 이름**(프로젝트 폴더명)을 묻는다.
- **트랙별 디렉토리 생성** (아래 "프로젝트 폴더 구조"):
  - 공통: `planning`, `chapters`, `book/front|body|back`, `assets`, `questions/pending|done`, `review`
  - `code`: 추가로 `code/`, `versions/`
  - `concept`: 추가로 `examples/` (그리고 `code/`·`versions/`는 만들지 않음)
  - `lecture`: 추가로 `sources/` (강별 `sources/NN강/`에 판서대본·자막을 둔다)
- `.claude/progress-template.json`을 복사해 `projects/[책이름]/progress.json` 생성 후 `track`을 확정값으로 설정한다.
- **트랙별 소스 입력 안내**:
  - `code`: `code/`에 완성 코드를 넣거나 GitHub URL을 달라.
  - `concept`: 목차가 있으면 주고, 없으면 주제만 알려달라.
  - `lecture`: 강별 `판서대본.md`를 `sources/NN강/판서대본.md`로 넣고, 유튜브 업로드가 끝났으면 영상 URL을 달라(자막은 `자막 추출` 명령으로 가져온다).
- 소스 사전 스캔 → STEP 1(씨앗 심기) 순서로 진행한다.
```

- [ ] **Step 2: lecture 트랙 명령어 라우팅 추가**

`CLAUDE.md`의 명령어 섹션에서, 기존 `### 코드 분석`/`### 시나리오 설계` 항목들 사이(또는 근처)에 lecture 트랙 명령어 3개를 추가한다:

```markdown
### `자막 추출 [강번호 또는 URL]`  (lecture 트랙)
- `lecture-caption` 스킬을 로드한다.
- yt-dlp로 유튜브 자동자막(.vtt)을 받아 → 정제 → 판서대본과 정렬한다.
- 산출물: `sources/NN강/caption-raw.vtt`, `caption-clean.md`, `정렬표.md`
- 자막이 필요 없으면 건너뛴다(대본 단독 진행 가능).

### `강의 분석`  (lecture 트랙 STEP 2)
- `track`이 `lecture`일 때의 STEP 2. `workflow/step2-강의해부.md` 참조. `lecture` 스킬 로드.
- 강의 자료(판서대본 + 자막)를 의도 필터로 분석 — 개념·비유·선수관계·오개념 추출.
- 산출물: `planning/lecture-analysis.md`

### `챕터 매핑`  (lecture 트랙 STEP 3)
- `track`이 `lecture`일 때의 STEP 3. `workflow/step3-챕터매핑.md` 참조.
- 강(講)을 책의 부(Part)/장(Chapter)으로 배열한다(개념 누적 구성).
- 산출물: `planning/chapter-map.md`
```

- [ ] **Step 3: STEP 2·3 분기 규칙을 라우팅에 명시**

기존 `### 코드 분석`(STEP 2)과 `### 시나리오 설계`(STEP 3) 설명에 트랙 분기 한 줄을 덧붙인다. 각 항목의 첫 줄 아래에 추가:

```markdown
> **트랙 분기**: STEP 2는 `code`→코드 분석(step2-코드해부), `concept`→개념 분석(step2-코드해부 내 개념 분기), `lecture`→`강의 분석`(step2-강의해부). STEP 3은 `code`/`concept`→시나리오 설계(step3-시나리오), `lecture`→`챕터 매핑`(step3-챕터매핑).
```

- [ ] **Step 4: 트랙 변수 표를 3값으로 확장**

`CLAUDE.md`의 "상수" 영역에서 "트랙 (변수 …)" 표를 찾아 3행으로 교체한다:

```markdown
| 트랙 | 재료 | 적용 |
|------|------|------|
| `lecture` | 강의 자료(판서대본 + 유튜브 자막) | STEP 2=강의해부, STEP 3=챕터매핑, 입력=`sources/` |
| `code`(기본) | 완성 코드 | STEP 2=코드해부, STEP 3=시나리오+버전, 입력=`code/`+`versions/` |
| `concept` | 목차/주제 | STEP 2·3·4 재료 단계만 분기, 입력=`examples/` |
```

- [ ] **Step 5: 트랙 종속 상수 표에 lecture 열 추가**

"트랙 종속 상수" 표(현재 `코드 트랙`/`개념 트랙` 2열)에 `강의 트랙` 열을 추가한다:

```markdown
| 상수 | 코드 트랙 | 개념 트랙 | 강의 트랙 |
|------|----------|----------|----------|
| 재료 | 완성 코드 | 목차/주제 | 판서대본 + 자막 |
| 성장 방식 | 버전별 성장(챕터=한 버전) | 의존성 누적 | 개념 누적(한 강=한 장) |
| 기술 파트 채움 | 실습/설명/참고 코드 | 수식·계산예제·다이어그램 | 용어정의·원리근거·(필요시)교육용코드·자막인용 |
| 자료 분류 | [실습/설명/참고] | [핵심수식/계산예제/그림/연습] | [개념/비유/선수/오개념] |
| 코드 비중 | 낮음 | 없음 | 낮음 |
```

- [ ] **Step 6: 참조 표에 lecture 워크플로우/스킬 추가**

CLAUDE.md 하단 "참조" 표 또는 스킬 섹션에 한 줄 추가:

```markdown
| `workflow/step2-강의해부.md`, `step3-챕터매핑.md` | lecture 트랙 STEP 2·3 가이드 |
| `.claude/skills/lecture/`, `lecture-caption/` | lecture 트랙 전용 스킬 |
```

- [ ] **Step 7: 검증 — 3트랙·lecture 라우팅이 CLAUDE.md에 일관되게 존재**

Run:
```bash
C="/c/Users/ssarm/Documents/books/book-workflow/CLAUDE.md"
echo "track values:"; grep -o "track=\`*\(lecture\|code\|concept\)" "$C" | sort -u
echo "lecture commands:"; grep -n "자막 추출\|강의 분석\|챕터 매핑\|step2-강의해부\|step3-챕터매핑" "$C"
echo "track-first:"; grep -n "STEP 0 — 트랙 선택" "$C"
```
Expected: lecture/code/concept 세 값 모두 등장, lecture 명령 3개 + 가이드 2개 라우팅 존재, "STEP 0 — 트랙 선택" 1건.

- [ ] **Step 8: Commit**

```bash
cd /c/Users/ssarm/Documents/books/book-workflow
git add CLAUDE.md
git commit -m "feat(claude): 시작 시 3-트랙 선택 + lecture 라우팅·상수 추가

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 5: step1-씨앗.md — lecture 트랙 분기 추가

**Files:**
- Modify: `workflow/step1-씨앗.md`

**Interfaces:**
- Consumes: `progress.json.track` (Task 4가 설정).
- Produces: lecture 트랙의 STEP 1 소스 스캔 절차. 산출물은 기존과 동일 `planning/seed.md`.

- [ ] **Step 1: 현재 트랙 분기 섹션 확인**

Run:
```bash
sed -n '1,40p' /c/Users/ssarm/Documents/books/book-workflow/workflow/step1-씨앗.md
```
Expected: "## 트랙 분기" 섹션에서 `code`→코드 사전 스캔, `concept`→목차/주제 스캔으로 갈리는 구조 확인. lecture 분기는 아직 없음.

- [ ] **Step 2: 트랙 분기에 lecture 항목 추가**

"## 트랙 분기"에서 `code`/`concept` 분기 목록 바로 아래에 lecture 줄을 추가한다:

```markdown
- `lecture`: "코드 사전 스캔" 대신 아래 "강의 사전 스캔"을 진행한다.
```

- [ ] **Step 3: "강의 사전 스캔" 하위 섹션 추가**

"### 코드 사전 스캔" 섹션과 같은 계층에, lecture 전용 스캔 섹션을 추가한다:

```markdown
### 강의 사전 스캔 (강의 트랙)

`sources/` 아래 강별 자료를 훑어 STEP 1 질문의 선택지에 맥락을 입힌다.

1. `sources/NN강/판서대본.md`를 모두 훑어 강 수·주제·등장 개념을 파악한다(1차 소스).
2. `sources/NN강/caption-clean.md`가 있으면 함께 훑어 즉흥 설명·실제 예시를 보강한다(2차 소스, 없으면 생략).
3. 파악한 강 목록·핵심 개념을 바탕으로 6개 씨앗 질문의 선택지를 구체화한다.
4. 자막이 아직 없으면 "대본 단독으로 진행 가능, 필요하면 `자막 추출`"을 안내한다.
```

- [ ] **Step 4: 검증**

Run:
```bash
grep -n "lecture\|강의 사전 스캔\|sources/" /c/Users/ssarm/Documents/books/book-workflow/workflow/step1-씨앗.md
```
Expected: lecture 분기 줄 + "강의 사전 스캔" 섹션 + `sources/` 참조가 보인다.

- [ ] **Step 5: Commit**

```bash
cd /c/Users/ssarm/Documents/books/book-workflow
git add workflow/step1-씨앗.md
git commit -m "feat(step1): lecture 트랙 강의 사전 스캔 분기 추가

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 6: step4-뼈대.md — lecture 자료 배치 분기 추가

**Files:**
- Modify: `workflow/step4-뼈대.md`

**Interfaces:**
- Consumes: `progress.json.track`, lecture STEP 2·3 산출물(`planning/lecture-analysis.md`, `planning/chapter-map.md`).
- Produces: lecture 트랙의 목차·자료 배치 절차. 산출물은 기존과 동일 `planning/outline.md`.

- [ ] **Step 1: 현재 트랙 분기 주석 확인**

Run:
```bash
sed -n '1,8p' /c/Users/ssarm/Documents/books/book-workflow/workflow/step4-뼈대.md
grep -n "자료 분류 (개념 트랙)\|트랙 분기" /c/Users/ssarm/Documents/books/book-workflow/workflow/step4-뼈대.md
```
Expected: 상단에 `> **트랙 분기**: code면 코드 실습 분류, concept면 "자료 분류(개념 트랙)"` 류의 주석, 그리고 "## 자료 분류 (개념 트랙)" 섹션 위치 확인.

- [ ] **Step 2: 트랙 분기 주석에 lecture 추가**

상단 `> **트랙 분기**: …` 주석을 lecture를 포함하도록 보완한다(기존 문구 끝에 이어 붙임):

```markdown
> **트랙 분기**: `code`면 코드 실습 분류, `concept`면 "자료 분류(개념 트랙)", `lecture`면 맨 끝 "강의 자료 배치(강의 트랙)" 섹션으로.
```

- [ ] **Step 3: "강의 자료 배치 (강의 트랙)" 섹션을 파일 끝에 추가**

```markdown
## 강의 자료 배치 (강의 트랙)

`planning/chapter-map.md`(강→장 배열)와 `planning/lecture-analysis.md`(개념·비유·선수·오개념)를 바탕으로 챕터별 목차를 세운다.

1. **장별 개념 누적 확인**: 각 장이 앞 장 개념 위에 쌓이는지(L3 선수관계)로 순서를 검증한다.
2. **비유 → 정의 배치**: 강의에서 쓴 비유(L2)를 이야기 파트에, 정식 정의를 기술 파트에 배치한다.
3. **오개념 배치**: 각 장의 오개념(L4)을 이야기 파트 "흔한 착각 → 교정"에 둔다.
4. **자료 슬롯 표시**: 슬라이드 캡처·판서 스크린샷·시뮬레이터 화면이 들어갈 자리를 `[CAPTURE NEEDED]`로 잡아둔다.
5. **갭 분석**: 강의에 없는데 책에 필요한 연결 고리를 찾아 보강 메모를 남긴다.
```

- [ ] **Step 4: 검증**

Run:
```bash
grep -n "lecture\|강의 자료 배치\|chapter-map\|lecture-analysis" /c/Users/ssarm/Documents/books/book-workflow/workflow/step4-뼈대.md
```
Expected: 트랙 분기 주석의 lecture + "강의 자료 배치" 섹션 + 두 산출물 경로 참조.

- [ ] **Step 5: Commit**

```bash
cd /c/Users/ssarm/Documents/books/book-workflow
git add workflow/step4-뼈대.md
git commit -m "feat(step4): lecture 트랙 강의 자료 배치 분기 추가

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 7: step5-챕터집필.md — lecture 기술 파트 분기 추가

**Files:**
- Modify: `workflow/step5-챕터집필.md`

**Interfaces:**
- Consumes: `progress.json.track`, `sources/NN강/`(판서대본+자막).
- Produces: lecture 트랙 챕터 집필 절차. 산출물은 기존과 동일 `chapters/NN-제목.md`.

- [ ] **Step 1: 현재 트랙 분기 주석 확인**

Run:
```bash
grep -n "트랙 분기\|기술 파트 (개념 트랙)\|기술 파트(개념" /c/Users/ssarm/Documents/books/book-workflow/workflow/step5-챕터집필.md
sed -n '80,100p' /c/Users/ssarm/Documents/books/book-workflow/workflow/step5-챕터집필.md
```
Expected: `> **트랙 분기**: 이야기 파트는 양 트랙 공유, 기술 파트만 분기` 주석과 code/concept 기술 파트 구조 확인.

- [ ] **Step 2: 트랙 분기 주석에 lecture 줄 추가**

기존 트랙 분기 주석 목록에 lecture 줄을 추가한다:

```markdown
> - `lecture`: 아래 "기술 파트 (강의 트랙)" 구조. 한 챕터의 원료 = 해당 강의 판서대본 + 자막 + (있으면) 시뮬레이터 캡처.
```

- [ ] **Step 3: "기술 파트 (강의 트랙)" 섹션 추가**

code/concept 기술 파트 섹션과 같은 계층에 lecture 기술 파트를 추가한다:

```markdown
### 기술 파트 (강의 트랙)

1. **용어 정리**: 이야기 파트에서 비유로 넘긴 용어를 정식 정의한다(비유 → 정의 필수).
2. **원리·근거**: "왜 그런가"를 강의 흐름대로 설명한다. 비유로 끝내지 말고 원리·근거(좋은 책·논문·인지심리)를 녹인다.
3. **자막 디테일 살리기**: 자막에서 강사가 즉흥으로 든 실제 예시·표현을 본문에 살린다(필러어는 버린다).
4. **(필요 시) 교육용 코드/수식**: 강의에 코드·수식이 있으면 기술 파트에서만, 교육용으로 제시한다.
5. **자료 삽입**: 슬라이드·판서·시뮬레이터 화면을 `![설명](경로)` 또는 `[CAPTURE NEEDED]`로 배치한다.
6. **더 알아보기**: 해당 강 주제의 심화 갈래를 한두 줄로 안내한다.
```

- [ ] **Step 4: 검증**

Run:
```bash
grep -n "lecture\|기술 파트 (강의 트랙)\|판서대본\|자막" /c/Users/ssarm/Documents/books/book-workflow/workflow/step5-챕터집필.md
```
Expected: lecture 분기 줄 + "기술 파트 (강의 트랙)" 섹션 + 판서대본/자막 참조.

- [ ] **Step 5: Commit**

```bash
cd /c/Users/ssarm/Documents/books/book-workflow
git add workflow/step5-챕터집필.md
git commit -m "feat(step5): lecture 트랙 기술 파트 분기 추가

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 8: README.md — 3-트랙 소개로 갱신 + 최종 일관성 검증

**Files:**
- Modify: `README.md`

**Interfaces:**
- Consumes: 전 태스크 결과(3-트랙 구조).
- Produces: 없음(문서). 저장소 첫인상.

- [ ] **Step 1: README 상단 소개를 3-트랙으로 교체**

`README.md` 상단의 "## 시작하기" 직전(또는 첫 문단 다음)에 3-트랙 안내를 추가한다:

```markdown
## 3가지 트랙

시작하면 셋 중 하나를 고른다.

| 트랙 | 무엇으로 쓰나 | 입력 |
|------|--------------|------|
| **유튜브 강의** (`lecture`) | 녹화된 강의(판서대본 + 유튜브 자막) | `sources/NN강/` |
| **소스코드** (`code`) | 완성된 코드 | `code/` + `versions/` |
| **목차/주제** (`concept`) | 완성물 없이 목차·주제만 | `examples/` |

`새 책 만들기`를 입력하면 가장 먼저 트랙을 묻고, 트랙에 맞는 STEP 2·3로 분기한다.
```

- [ ] **Step 2: 명령어 표에 lecture 명령 추가**

README의 명령어 표에 lecture 트랙 명령을 추가한다(기존 표 형식에 맞춰):

```markdown
| `자막 추출 [강/URL]` | — | (lecture) 유튜브 자막 → 소스 (선택) |
| `강의 분석` | 2 | (lecture) 강의 해부 |
| `챕터 매핑` | 3 | (lecture) 강 → 부/장 배열 |
```

- [ ] **Step 3: 전체 일관성 종단 검증 (3트랙 분기 무결성)**

Run:
```bash
cd /c/Users/ssarm/Documents/books/book-workflow
echo "== 트랙별 STEP2/3 가이드 존재 =="
ls workflow/step2-코드해부.md workflow/step3-시나리오.md workflow/step2-강의해부.md workflow/step3-챕터매핑.md
echo "== lecture 스킬 존재 =="
ls .claude/skills/lecture/SKILL.md .claude/skills/lecture-caption/SKILL.md
echo "== 세 트랙 값이 CLAUDE.md에 모두 등장 =="
for t in lecture code concept; do grep -q "$t" CLAUDE.md && echo "$t OK"; done
echo "== step1/4/5에 lecture 분기 존재 =="
for f in step1-씨앗 step4-뼈대 step5-챕터집필; do grep -q "lecture\|강의" workflow/$f.md && echo "$f OK"; done
echo "== 깨진 참조: CLAUDE.md가 가리키는 lecture 가이드 실재 =="
grep -q "step2-강의해부" CLAUDE.md && test -f workflow/step2-강의해부.md && echo "ref OK"
```
Expected: 모든 `OK`/`ref OK` 출력, 누락 없음.

- [ ] **Step 4: 기존 코드/개념 트랙 회귀 없음 확인 (삭제 금지 자산)**

Run:
```bash
cd /c/Users/ssarm/Documents/books/book-workflow
ls .claude/skills/code .claude/skills/math .claude/skills/image-gen \
   workflow/step2-코드해부.md workflow/step3-시나리오.md && echo "기존 자산 보존 OK"
git log --oneline feature/개념트랙-이미지자동화..HEAD
```
Expected: "기존 자산 보존 OK" + Task1~8 커밋들이 새 브랜치에만 쌓여 있음.

- [ ] **Step 5: Commit**

```bash
cd /c/Users/ssarm/Documents/books/book-workflow
git add README.md
git commit -m "docs(readme): 3-트랙(유튜브/코드/목차) 소개·명령 갱신

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## 마무리 (구현 완료 후)

- 8개 태스크 완료 후 `superpowers:finishing-a-development-branch` 스킬로 통합 방식(merge/PR/cleanup)을 결정한다.
- lecture 트랙 실사용 전 환경 준비: `pip install -U yt-dlp` (자막 추출용). 자막 없이 대본 단독 진행도 가능.
- 선택적 후속(YAGNI — 지금은 안 함): workflow를 트랙별 하위폴더로 재편(접근법 C).
