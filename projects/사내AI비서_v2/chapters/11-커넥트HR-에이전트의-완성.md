# 챕터 11. 커넥트HR 에이전트의 완성. 파이프라인과 근거, 그리고 마지막 여정

:::goal
**이번 챕터가 끝나면**

- 챕터 8~10에서 만든 함수들을 **하나의 파이프라인**으로 조립합니다
- 답변에 **근거**를 붙여 줍니다. 정형 질문은 JSON, 비정형·복합 질문은 **문서 페이지 이미지**
- 챕터 7의 채팅 UI를 재사용해서 **실제 사용자 경험**으로 완성합니다
- 챕터 1~10의 모든 박스가 채워진 **커넥트HR 에이전트의 전체 구성도**를 확인합니다
:::

::::prep
**준비하기**. 실습 시작 전 한 번만 설정

### 1. 실습 폴더 이동

```bash [터미널] 폴더 이동
cd rag-start/ex11
```

파일 구조는 다음과 같습니다.

```text ex11 디렉토리
ex11/
├── run.py                       # [참고] FastAPI 서버 실행 (챕터 7 UI 재사용)
├── app/                         # [참고] 채팅 API + DB
│   ├── main.py
│   ├── chat_api.py              # [실습] 근거 분기 로직 추가
│   └── database.py
├── src/
│   ├── pipeline.py              # [실습] Application Layer. RagPipeline 조립
│   ├── evidence_capture.py      # [실습] PDF 페이지 → PNG 캡처 근거
│   ├── evidence.py              # [참고] 출처 추출
│   ├── agent.py                 # [참고] IntegratedAgent (챕터 6)
│   ├── tuning/                  # [참고] Domain Layer. 튜닝 부품 객체들
│   │   ├── query_expander.py    # QueryExpander — 약어 사전
│   │   ├── bm25_retriever.py    # BM25Retriever — rank-bm25 래퍼
│   │   ├── ensemble_retriever.py # EnsembleRetriever — BM25 + ChromaDB 하이브리드
│   │   ├── reranker.py          # CrossEncoderReranker (+ SimpleReranker 폴백)
│   │   ├── parent_doc.py        # ParentDocumentRetriever — 자식 → 부모 복원 (챕터 9)
│   │   └── hybrid_parser.py     # 챕터 10 파서 이식 — 인덱싱 시 pypdf → Vision
│   └── tools/                   # [참고] Infrastructure Layer. SQL 3종 + ChromaDB 검색
│       ├── leave_balance.py
│       ├── sales_sum.py
│       ├── list_employees.py
│       └── search_documents.py
├── templates/chat.html          # [실습] 근거 이미지·JSON 표시 영역 추가
├── static/
│   └── evidence/                # 런타임 생성 (PDF 캡처 PNG 캐시)
└── data/
    ├── docs/                    # [참고] 원본 PDF
    ├── chroma_db/               # 런타임 생성
    └── test_questions.json      # [참고]
```

### 2. 실습 환경 구축

```bash [터미널] 환경 구성. macOS / Linux
cd ex11
python3.12 -m venv .venv
source .venv/bin/activate
cp .env.example .env
ollama pull llama3.1:8b
ollama pull qwen2.5vl:7b
pip install -r requirements.txt
```

```bash [터미널] 환경 구성. Windows
cd ex11
py -3.12 -m venv .venv
.venv\Scripts\activate
copy .env.example .env
ollama pull llama3.1:8b
ollama pull qwen2.5vl:7b
pip install -r requirements.txt
```

### 3. 실습 순서

1. `src/pipeline.py` (D 조합: Semantic 청킹, 하이브리드 검색, 리랭커, 약어 확장, Parent Doc, Vision 파서)을 하나로 조립합니다
2. `src/evidence_capture.py`. PDF 페이지를 PNG로 렌더링해 근거로 제공합니다
3. `app/chat_api.py` + `templates/chat.html`. 정형(JSON)과 비정형(이미지) 근거 분기를 붙입니다
::::

## 11.1 파이프라인 조립 - 튜닝 부품을 한 파일에 엮는다

팀장이 옆자리로 의자를 밀고 왔습니다. 모니터 한 켠엔 챕터 8~10에서 돌린 실험 결과가 여전히 떠 있었습니다.

**팀장**: "이번엔 새 이론을 얹지 말고, 지금까지 본 튜닝을 한 자리에 모아 이어 붙이기만 해요."

오픈이가 에디터에서 새 파일을 열었습니다. pipeline.py. 키보드 위에 손을 올린 채 빈 화면을 잠시 바라봤습니다. 이번 장의 할 일은 지금까지 따로 실험하던 **튜닝 부품**을 한 파일에 모아 질의 흐름 하나로 엮는 것입니다. 챕터 8·9에서는 조각별로 돌려 성능을 비교만 했다면, 여기서는 같은 개념을 함수로 `pipeline.py` 안에 모아 `run_pipeline` 하나로 차례차례 호출합니다.

<div style="background:var(--color-surface);border:1px solid var(--color-border);border-radius:var(--radius-lg);padding:28px 20px;margin:var(--space-xl) 0">
<div style="font-size:12px;font-weight:700;color:var(--color-text-muted);letter-spacing:1px;margin-bottom:20px;text-align:center">그림 11-1. RAG 엔진. 질문이 들어와 답변이 나가기까지</div>

<!-- 입력 포트 -->
<div style="text-align:center">
<div style="display:inline-block;padding:6px 16px;background:var(--color-surface-alt);border:1px dashed var(--color-border);border-radius:20px;font-size:11px;font-weight:600;color:var(--color-text-muted)">사용자 질문</div>
<div style="font-size:14px;color:var(--color-text-subtle);margin:2px 0">▼</div>
</div>

<!-- 엔진 본체: 4단계 파이프라인 -->
<div style="border:2px solid var(--color-border);border-radius:12px;padding:18px 14px;background:var(--color-bg);max-width:900px;margin:0 auto;position:relative">
<div style="position:absolute;top:-10px;left:18px;background:var(--color-surface);padding:0 8px;font-size:10px;font-weight:700;color:var(--color-text-muted);letter-spacing:1px">RAG ENGINE</div>

<div style="display:grid;grid-template-columns:1fr 22px 1fr 22px 1fr 22px 1fr;gap:6px;align-items:stretch">

<!-- 1. QueryExpander -->
<div style="background:var(--color-surface);border:1px solid var(--color-border);border-radius:8px;padding:10px 12px">
<div style="display:flex;align-items:center;gap:6px;margin-bottom:4px">
<div style="width:20px;height:20px;border-radius:50%;background:var(--color-accent);color:white;font-size:10px;font-weight:700;display:flex;align-items:center;justify-content:center">1</div>
<div style="font-size:12px;font-weight:700;color:var(--color-text-heading)">QueryExpander</div>
</div>
<div><code style="font-size:9px;background:var(--color-surface-alt);padding:1px 5px;border-radius:3px;color:var(--color-text)">expand()</code></div>
<div style="font-size:10px;color:var(--color-text-subtle);margin-top:4px">약어 확장</div>
</div>

<div style="display:flex;align-items:center;justify-content:center;font-size:18px;color:var(--color-accent);font-weight:700">→</div>

<!-- 2. EnsembleRetriever -->
<div style="background:var(--color-surface);border:1px solid var(--color-border);border-radius:8px;padding:10px 12px">
<div style="display:flex;align-items:center;gap:6px;margin-bottom:4px">
<div style="width:20px;height:20px;border-radius:50%;background:var(--color-accent);color:white;font-size:10px;font-weight:700;display:flex;align-items:center;justify-content:center">2</div>
<div style="font-size:12px;font-weight:700;color:var(--color-text-heading)">EnsembleRetriever</div>
</div>
<div><code style="font-size:9px;background:var(--color-surface-alt);padding:1px 5px;border-radius:3px;color:var(--color-text)">search()</code></div>
<div style="font-size:10px;color:var(--color-text-subtle);margin-top:4px">BM25 + Vector</div>
</div>

<div style="display:flex;align-items:center;justify-content:center;font-size:18px;color:var(--color-accent);font-weight:700">→</div>

<!-- 3. CrossEncoderReranker -->
<div style="background:var(--color-surface);border:1px solid var(--color-border);border-radius:8px;padding:10px 12px">
<div style="display:flex;align-items:center;gap:6px;margin-bottom:4px">
<div style="width:20px;height:20px;border-radius:50%;background:var(--color-accent);color:white;font-size:10px;font-weight:700;display:flex;align-items:center;justify-content:center">3</div>
<div style="font-size:12px;font-weight:700;color:var(--color-text-heading)">CrossEncoderReranker</div>
</div>
<div><code style="font-size:9px;background:var(--color-surface-alt);padding:1px 5px;border-radius:3px;color:var(--color-text)">rerank()</code></div>
<div style="font-size:10px;color:var(--color-text-subtle);margin-top:4px">딥러닝 채점</div>
</div>

<div style="display:flex;align-items:center;justify-content:center;font-size:18px;color:var(--color-accent);font-weight:700">→</div>

<!-- 4. ParentDocumentRetriever -->
<div style="background:var(--color-surface);border:1px solid var(--color-border);border-radius:8px;padding:10px 12px">
<div style="display:flex;align-items:center;gap:6px;margin-bottom:4px">
<div style="width:20px;height:20px;border-radius:50%;background:var(--color-accent);color:white;font-size:10px;font-weight:700;display:flex;align-items:center;justify-content:center">4</div>
<div style="font-size:12px;font-weight:700;color:var(--color-text-heading)">ParentDocumentRetriever</div>
</div>
<div><code style="font-size:9px;background:var(--color-surface-alt);padding:1px 5px;border-radius:3px;color:var(--color-text)">resolve()</code></div>
<div style="font-size:10px;color:var(--color-text-subtle);margin-top:4px">자식 → 부모 복원</div>
</div>

</div>

<!-- 외부 의존 (2·3번 아래) -->
<div style="display:grid;grid-template-columns:1fr 22px 1fr 22px 1fr 22px 1fr;gap:6px;margin-top:10px">
<div></div>
<div></div>
<div style="text-align:center;font-size:10px;color:var(--color-text-subtle);line-height:1.5">
<div style="font-size:12px;color:var(--color-text-muted)">↑</div>
<code style="font-size:9px;background:var(--color-surface-alt);padding:1px 5px;border-radius:3px">ChromaDB</code> · <code style="font-size:9px;background:var(--color-surface-alt);padding:1px 5px;border-radius:3px">rank-bm25</code>
</div>
<div></div>
<div style="text-align:center;font-size:10px;color:var(--color-text-subtle);line-height:1.5">
<div style="font-size:12px;color:var(--color-text-muted)">↑</div>
<code style="font-size:9px;background:var(--color-surface-alt);padding:1px 5px;border-radius:3px">CE 모델</code>
</div>
<div></div>
<div></div>
</div>

</div>

<!-- 출력 포트 -->
<div style="text-align:center;margin-top:12px">
<div style="font-size:14px;color:var(--color-text-subtle)">▼</div>
<div style="display:inline-block;padding:6px 16px;background:var(--color-surface-alt);border:1px dashed var(--color-border);border-radius:20px;font-size:11px;font-weight:600;color:var(--color-text-muted)">상위 k개 컨텍스트 → LLM 답변 생성</div>
</div>

<div style="margin-top:18px;padding-top:12px;border-top:1px solid var(--color-border-dashed);font-size:11px;color:var(--color-text-muted);line-height:1.6;text-align:center">최초 실행 시 <code style="font-size:10px;background:var(--color-surface-alt);padding:1px 5px;border-radius:3px">ex11/data/docs</code>를 하이브리드 파서(pypdf → Vision)로 인덱싱해 <b>자식 청크는 ChromaDB</b>에, <b>부모 페이지는 parent_docs.json</b>에 나눠 적재합니다. 네 부품은 질문이 들어올 때마다 자식 검색 → 부모 복원 순으로 실행됩니다</div>
</div>

파이프라인을 **레이어드 아키텍처**로 분리합니다. `pipeline.py`의 `RagPipeline`이 질의가 들어올 때마다 네 부품(`QueryExpander`·`EnsembleRetriever`·`CrossEncoderReranker`·`ParentDocumentRetriever`)을 차례대로 부르고, 그 부품들은 `src/tools/`의 ChromaDB·BM25 같은 외부 저장소를 끌어 씁니다. 벡터DB는 `ex11/data/docs`를 자체 인덱싱해 **자식·부모 두 저장소**로 빌드합니다. 페이지 단위 원문은 부모로 `ex11/data/parent_docs.json`에 두고, 220자 정도로 잘게 쪼갠 자식 청크는 ChromaDB에 `parent_id` 메타데이터와 함께 적재합니다. PDF는 `src/tuning/hybrid_parser.py`(챕터 10의 하이브리드 파서를 이식한 모듈)가 pypdf로 먼저 꺼내 보고 스캔본이면 Vision LLM으로 넘어가 텍스트를 뽑습니다. BM25 인덱스와 Cross-Encoder 모델은 `EnsembleRetriever`·`CrossEncoderReranker` 객체가 최초 사용 시 1회만 초기화합니다.

<div style="background:var(--color-surface);border:1px solid var(--color-border);border-radius:var(--radius-lg);padding:24px 20px;margin:var(--space-xl) 0">
<div style="font-size:12px;font-weight:700;color:var(--color-text-muted);margin-bottom:20px;text-align:center;letter-spacing:1px">레이어드 아키텍처. 위에서 아래로 호출·참조만 흐릅니다</div>

<div style="display:grid;grid-template-columns:100px 1fr;gap:10px 14px;align-items:center;max-width:820px;margin:0 auto;font-size:12px;line-height:1.55">

<!-- Application -->
<div style="text-align:right;font-size:10px;font-weight:700;color:var(--color-accent-text);letter-spacing:1px">APPLICATION<div style="font-size:10px;font-weight:400;color:var(--color-text-muted);letter-spacing:0">지휘</div></div>
<div style="background:var(--color-surface-alt);border:1px solid var(--color-border);border-radius:8px;padding:10px 14px;display:flex;align-items:center;gap:10px">
<code style="font-size:10px;background:var(--color-surface);padding:1px 6px;border-radius:3px;color:var(--color-text);border:1px solid var(--color-border)">src/pipeline.py</code>
<span style="font-weight:700;color:var(--color-text-heading)">RagPipeline</span>
<span style="color:var(--color-text-muted);font-size:11px">— 네 부품을 순서대로 호출</span>
</div>

<!-- 화살표 -->
<div></div>
<div style="color:var(--color-text-subtle);font-size:11px;padding-left:6px">↓ <span style="font-style:italic">호출</span></div>

<!-- Domain (흐름 강조) -->
<div style="text-align:right;font-size:10px;font-weight:700;color:var(--color-info-text);letter-spacing:1px">DOMAIN<div style="font-size:10px;font-weight:400;color:var(--color-text-muted);letter-spacing:0">부품</div></div>
<div style="border:1px solid var(--color-border);border-radius:8px;padding:10px 12px">
<div style="font-size:10px;color:var(--color-text-subtle);margin-bottom:6px"><code style="font-size:10px;background:var(--color-surface-alt);padding:1px 5px;border-radius:3px">src/tuning/</code> 안의 네 파일 · 질의가 들어올 때마다 ①→②→③→④ 순</div>
<div style="display:grid;grid-template-columns:1fr 14px 1fr 14px 1fr 14px 1fr;gap:4px;align-items:stretch;font-size:11px">
<div style="background:var(--color-surface-alt);border:1px solid var(--color-border);border-radius:6px;padding:6px 8px">
<div style="font-size:10px;color:var(--color-text-subtle)">①</div>
<div style="font-weight:700;color:var(--color-text-heading)">QueryExpander</div>
<div style="font-size:10px;color:var(--color-text-subtle)">약어 확장</div>
</div>
<div style="display:flex;align-items:center;justify-content:center;color:var(--color-accent);font-weight:700">→</div>
<div style="background:var(--color-surface-alt);border:1px solid var(--color-border);border-radius:6px;padding:6px 8px">
<div style="font-size:10px;color:var(--color-text-subtle)">②</div>
<div style="font-weight:700;color:var(--color-text-heading)">EnsembleRetriever</div>
<div style="font-size:10px;color:var(--color-text-subtle)">BM25+Vector</div>
</div>
<div style="display:flex;align-items:center;justify-content:center;color:var(--color-accent);font-weight:700">→</div>
<div style="background:var(--color-surface-alt);border:1px solid var(--color-border);border-radius:6px;padding:6px 8px">
<div style="font-size:10px;color:var(--color-text-subtle)">③</div>
<div style="font-weight:700;color:var(--color-text-heading)">CrossEncoderReranker</div>
<div style="font-size:10px;color:var(--color-text-subtle)">딥러닝 리랭크</div>
</div>
<div style="display:flex;align-items:center;justify-content:center;color:var(--color-accent);font-weight:700">→</div>
<div style="background:var(--color-surface-alt);border:1px solid var(--color-border);border-radius:6px;padding:6px 8px">
<div style="font-size:10px;color:var(--color-text-subtle)">④</div>
<div style="font-weight:700;color:var(--color-text-heading)">ParentDocumentRetriever</div>
<div style="font-size:10px;color:var(--color-text-subtle)">부모 복원</div>
</div>
</div>
</div>

<!-- 화살표 -->
<div></div>
<div style="color:var(--color-text-subtle);font-size:11px;padding-left:6px">↓ <span style="font-style:italic">②③④가 참조</span></div>

<!-- Infrastructure -->
<div style="text-align:right;font-size:10px;font-weight:700;color:var(--color-success-text);letter-spacing:1px">INFRASTRUCTURE<div style="font-size:10px;font-weight:400;color:var(--color-text-muted);letter-spacing:0">외부</div></div>
<div style="background:var(--color-surface-alt);border:1px solid var(--color-border);border-radius:8px;padding:10px 14px;display:flex;flex-wrap:wrap;align-items:center;gap:6px">
<code style="font-size:10px;background:var(--color-surface);padding:1px 6px;border-radius:3px;color:var(--color-text);border:1px solid var(--color-border)">src/tools/search_documents.py</code>
<span style="color:var(--color-text-muted);font-size:11px">중개 →</span>
<code style="font-size:10px;background:var(--color-surface);padding:1px 6px;border-radius:3px">ChromaDB</code>
<code style="font-size:10px;background:var(--color-surface);padding:1px 6px;border-radius:3px">rank-bm25</code>
<code style="font-size:10px;background:var(--color-surface);padding:1px 6px;border-radius:3px">sentence-transformers</code>
<code style="font-size:10px;background:var(--color-surface);padding:1px 6px;border-radius:3px">parent_docs.json</code>
</div>

</div>

<div style="margin-top:18px;padding-top:12px;border-top:1px solid var(--color-border-dashed);font-size:11px;color:var(--color-text-muted);line-height:1.7;text-align:center">
세 층이 단방향으로만 얽히기 때문에(<b>위→아래</b>), 한 층의 내부를 바꿔도 다른 층은 영향이 적습니다. 예) 벡터DB를 Qdrant로 갈아끼울 땐 Infrastructure만 고치면 됩니다
</div>
</div>

`src/pipeline.py`를 열고 TODO의 `pass`를 지우고 아래 코드를 작성합니다.

```python [실습 1] src/pipeline.py. RagPipeline 조립 (Application Layer)
from .tuning import (
    CrossEncoderReranker,
    EnsembleRetriever,
    ParentDocumentRetriever,
    QueryExpander,
)


class RagPipeline:
    # TODO: 4개 도메인 객체를 생성자에서 준비하고 run()에서 순서대로 호출합니다.
    def __init__(self, alpha: float = 0.5) -> None:
        from .tools.search_documents import _get_store
        collection, parent_docs = _get_store()
        # 1. 약어 확장 (챕터 9)
        self.expander = QueryExpander()
        # 2. BM25 + ChromaDB 하이브리드 검색 (챕터 8) — 자식 청크 대상
        self.retriever = EnsembleRetriever(collection, alpha=alpha) if collection else None
        # 3. Cross-Encoder 리랭크 (챕터 8)
        self.reranker = CrossEncoderReranker()
        # 4. 자식 → 부모 복원, 중복 parent_id 제거 (챕터 9)
        self.parent_retriever = (
            ParentDocumentRetriever(child_collection=collection, parent_docs=parent_docs)
            if collection and parent_docs else None
        )

    def run(self, query: str, k: int = 3) -> list[dict]:
        if self.retriever is None or self.parent_retriever is None:
            return []
        expanded = self.expander.expand(query)
        retrieved = self.retriever.search(expanded, k=k)          # 자식 청크
        if not retrieved:
            return []
        reranked = self.reranker.rerank(expanded, retrieved, top_k=k * 2)
        parents = self.parent_retriever.resolve(reranked, top_k=k)  # 부모 복원
        return parents[:k]
```

`RagPipeline.__init__`은 생성자 주입 패턴으로 도메인 객체를 한 번 준비해 두고, `run()`은 다섯 줄 안에서 **확장 → 자식 검색 → 리랭크 → 부모 복원 → 절단**을 호출합니다. 객체별 내부 구현은 `src/tuning/` 각 파일에 나뉘어 있어 개별 교체·테스트가 쉽습니다.

| 도메인 객체 (tuning/*) | 역할 |
|---------|------|
| `QueryExpander.expand(q)` | `WFH` → `WFH(재택근무)` 사내 약어 확장 |
| `EnsembleRetriever.search(q, k)` | BM25 + ChromaDB(Vector)로 자식 청크 검색. 두 점수 정규화 후 가중 합산 |
| `BM25Retriever.search(q, k)` | `rank-bm25` 키워드 검색기 (EnsembleRetriever 내부에서 사용) |
| `CrossEncoderReranker.rerank(q, docs, k)` | `BAAI/bge-reranker-v2-m3` 딥러닝 모델로 쿼리·자식 청크 정합성 채점 |
| `ParentDocumentRetriever.resolve(docs, k)` | 자식 청크의 `parent_id`로 부모 페이지 원문 복원. 같은 부모 중복 제거 |

## 11.2 웹 UI로 확인한다

조립이 끝났습니다. 선택한 D 조합(Semantic 청킹, 하이브리드 검색, 리랭킹, 약어 확장, Parent Doc, Vision 파서)을 얹은 파이프라인으로 챕터 7의 채팅 UI를 다시 띄워 보겠습니다.

```bash [터미널] ex11 서버 실행 (챕터 7 UI + 챕터 8~10 튜닝 적용)
python run.py
```

브라우저에서 `http://localhost:8000/chat`을 엽니다. 챕터 7의 ConnectHRAgent 채팅 UI가 뜹니다. 같은 UI, 다른 엔진.

오픈이가 채팅창으로 손을 옮겼습니다. 커서가 입력란에서 깜빡였습니다.

첫 질문. `병가 신청 절차 알려줘`.

병가 규정이 정확히 나왔습니다. 출장이 섞이지 않습니다. Semantic 청킹이 주제를 분리하고, 리랭커가 관련 없는 문서를 밀어낸 덕분입니다.

다음은 약어가 섞인 질문. `WFH 정책 알려줘`.

"재택근무는 입사 6개월 이상 정규직에 한해..." 약어 사전이 WFH를 재택근무로 바꿔 검색한 결과입니다.

마지막 질문. `정보보안서약서 내용이 뭐야?`.

한 박자 기다렸습니다. 노트북 팬이 숨을 한 번 몰아쉬고 서약서 본문이 그대로 돌아왔습니다. 제4조 생성형 AI 활용 지침, 제5조 외부 유출 금지, 조항 번호까지 또렷하게. 챕터 10에서 붙인 Vision 파서가 스캔본 한 장을 텍스트로 꺼내 벡터DB에 올려 뒀던 덕입니다.

*세 개 다 잡았네.*

<div style="background:var(--color-surface);border:1px solid var(--color-border);border-radius:var(--radius-lg);padding:var(--space-xl);margin:var(--space-xl) 0">
<div style="font-size:var(--fs-sm);font-weight:700;color:var(--color-accent-text);margin-bottom:var(--space-lg);text-align:center">그림 11-2. 웹 UI 테스트. 선택한 조합(D)으로 실행한 결과입니다</div>
<div style="display:flex;flex-direction:column;gap:var(--space-md)">
<div style="border:1px solid var(--color-border);border-radius:var(--radius-md);padding:var(--space-sm)">
<div style="font-size:var(--fs-xs);font-weight:600;margin-bottom:var(--space-xs)">"병가 신청 절차 알려줘"</div>
<div style="font-size:var(--fs-xs);color:var(--color-success-text)">병가 규정이 정확히 나옵니다. Semantic 청킹이 주제를 분리하고, 리랭커가 관련 없는 문서를 밀어냈습니다.</div>
</div>
<div style="border:1px solid var(--color-border);border-radius:var(--radius-md);padding:var(--space-sm)">
<div style="font-size:var(--fs-xs);font-weight:600;margin-bottom:var(--space-xs)">"WFH 정책 알려줘"</div>
<div style="font-size:var(--fs-xs);color:var(--color-success-text)">약어 사전이 WFH를 재택근무로 바꿔 검색했습니다.</div>
</div>
<div style="border:1px solid var(--color-border);border-radius:var(--radius-md);padding:var(--space-sm)">
<div style="font-size:var(--fs-xs);font-weight:600;margin-bottom:var(--space-xs)">"정보보안서약서 내용이 뭐야?"</div>
<div style="font-size:var(--fs-xs);color:var(--color-success-text)">Vision 파서가 스캔본 서약서 본문을 꺼내 둔 덕에 조항 번호까지 정확히 돌아왔습니다.</div>
</div>
</div>
<div style="margin-top:var(--space-sm);font-size:var(--fs-xs);color:var(--color-text-subtle)">세 질문 모두 D 조합으로 해결됩니다. 스캔본이 없는 환경이라면 Vision 파서를 빼고 C 조합으로도 충분합니다.</div>
</div>

**오픈이**: "병가·WFH·서약서 다 되네요. 챕터 10에서 Vision을 붙인 게 여기서 제 역할을 했어요."

**동료**: "서약서까지 읽을 줄은 몰랐는데요."

## 11.3 답변에 근거를 붙이자

조립한 파이프라인이 답은 잘 만들어 냈습니다. 그런데 테스트를 돌리던 팀장이 멈췄습니다.

**팀장**: "답은 맞는 것 같은데, 어느 페이지에서 가져왔는지 눈으로 확인이 안 돼요. 문장만 있으면 직원들이 '이거 진짜야?' 소리가 나와요."

**오픈이**: "아래에 출처 파일명이 링크로 붙긴 해요."

**팀장**: "링크 말고, 그 페이지를 **그대로 보여 주면** 좋겠는데요. PDF 원본이 바로 옆에 뜨는 식으로."

오픈이가 수첩에 적었습니다.

:::memo
**— 근거 제공 업그레이드 —**

1. **답변 옆에 원본 페이지**
   - 출처 파일명 + 페이지 번호만으로는 신뢰가 약하다
   - PDF 해당 페이지를 PNG로 **그대로** 보여 주기
2. **구현 방향**
   - PyMuPDF로 PDF 열기 → 페이지 렌더링 → PNG 저장
   - 한 번 만든 이미지는 캐시에 남겨 재사용
:::

검색 결과 한 건마다 `(source, page)` 쌍이 이미 붙어 있습니다. 그 정보로 PDF의 해당 페이지를 PNG로 떠 주면 끝입니다. `src/evidence_capture.py`를 열고 TODO의 `pass`를 지우고 아래 코드를 작성합니다.

```python [실습 1] src/evidence_capture.py. PDF 페이지 → PNG 렌더 + 캐시
import fitz  # PyMuPDF
from pathlib import Path

BASE_DIR = Path(__file__).resolve().parent.parent
DOCS_DIR = BASE_DIR / "data" / "docs"
CACHE_DIR = BASE_DIR / "static" / "evidence"


def render_evidence_image(source_name: str, page_num: int, dpi: int = 150) -> str:
    # TODO: PDF 특정 페이지를 PNG로 렌더링하고 /static/evidence/*.png URL 반환.
    # 1. data/docs/ 하위에서 PDF 경로 탐색
    pdf_path = next(DOCS_DIR.rglob(f"{source_name}.pdf"), None)
    if not pdf_path:
        return ""
    # 2. 캐시 디렉토리 준비
    CACHE_DIR.mkdir(parents=True, exist_ok=True)
    out_path = CACHE_DIR / f"{source_name}_p{page_num}.png"
    # 3. 캐시 적중 시 재사용
    if out_path.exists():
        return f"/static/evidence/{out_path.name}"
    # 4. PDF 열어서 해당 페이지 → PNG 저장
    doc = fitz.open(str(pdf_path))
    pix = doc[page_num - 1].get_pixmap(dpi=dpi)
    pix.save(str(out_path))
    doc.close()
    return f"/static/evidence/{out_path.name}"
```

매개변수를 보면 `source_name`은 `test_questions.json`의 `relevant_sources`와 같은 PDF 파일명(확장자 제외), `page_num`은 1부터 시작하는 페이지 번호입니다. `dpi`가 클수록 선명하지만 파일도 커집니다. 150이 화면 표시용으로는 충분합니다.

`/static/evidence/` 경로는 FastAPI가 정적 파일로 이미 서빙하고 있어서, 반환한 URL을 그대로 `<img src="...">`에 쓰면 브라우저에 뜹니다. 캐시 폴더에 한 번 저장된 이미지는 다음 호출에서 재사용되니 성능 부담도 없습니다.

<div style="background:var(--color-surface);border:1px solid var(--color-border);border-radius:var(--radius-lg);padding:24px;margin:var(--space-xl) 0">
<div style="font-size:var(--fs-sm);font-weight:700;color:var(--color-accent-text);margin-bottom:16px;text-align:center">그림 11-3. 답변 아래에 PDF 페이지 캡처가 붙는 UI</div>
<div style="border:1px solid var(--color-border);border-radius:var(--radius-md);padding:16px;background:var(--color-bg)">
<div style="font-size:13px;color:var(--color-text-heading);margin-bottom:10px"><b>질문:</b> 병가 신청 절차 알려줘</div>
<div style="padding:12px;background:var(--color-surface);border-radius:6px;font-size:13px;color:var(--color-text);margin-bottom:14px">병가는 진단서 제출 후 인사팀에 신청합니다. 3일 이상 연속 시 반드시 진단서가 필요하며, 연 30일까지 유급 처리됩니다.</div>
<div style="padding:10px 12px;background:var(--color-accent-bg);border:1px solid var(--color-accent-border);border-radius:6px">
<div style="font-size:11px;font-weight:700;color:var(--color-accent);margin-bottom:6px;letter-spacing:1px">근거 페이지 이미지 (1건)</div>
<div style="display:flex;gap:10px">
<div style="flex:0 0 140px;height:180px;background:linear-gradient(180deg,#ffffff 0%,#f1f5f9 100%);border:1px solid var(--color-border);border-radius:4px;display:flex;align-items:center;justify-content:center;font-size:11px;color:var(--color-text-subtle)">[PDF 페이지]<br>HR_취업규칙 p.3</div>
<div style="flex:1;font-size:12px;color:var(--color-text-muted);line-height:1.7">왼쪽은 <code>data/docs/hr/HR_취업규칙_원본.pdf</code>의 3페이지가 PNG로 렌더링된 결과입니다. 사용자가 클릭하면 크게 뜨고, 답변 문장이 어느 조항에서 나왔는지 눈으로 곧바로 확인됩니다.</div>
</div>
</div>
</div>
</div>

동료가 화면을 보고 말했습니다.

**동료**: "이제 '진짜야?' 소리가 안 나오겠네요."

## 11.4 정형과 비정형, 근거 모양을 다르게

근거 이미지를 붙였더니 문제가 하나 더 드러났습니다.

**동료**: "서약서나 취업규칙은 이렇게 이미지로 보는 게 좋은데, '1분기 매출 얼마야' 같은 질문은 이미지보다 숫자 그 자체가 더 편해요. 표라면 표, JSON이면 JSON으로 보여 주는 게 낫겠어요."

*맞는 말이네. 비정형 답은 문서가 근거지만, 정형 답은 수치가 근거다.*

질문의 성격에 따라 **근거의 모양**을 달리해야 합니다. 간단한 규칙으로도 충분합니다. 질문에 숫자·금액·수치·퍼센트 같은 단어가 있으면 **정형**, 그 외는 **비정형**. 정형이면 DB 조회 결과를 JSON 그대로 내보내고, 비정형이면 §11.3에서 만든 PDF 캡처 이미지를 붙입니다.

실무에서는 LLM에 "이 질문이 정형이냐 비정형이냐?"를 먼저 묻는 방식도 쓰지만, 호출이 한 번 더 늘어납니다. 이 책에서는 키워드 규칙으로 시작하고, 필요해지면 LLM 분류로 업그레이드하는 쪽으로 가겠습니다.

`app/chat_api.py`를 열고 TODO의 `pass`를 지우고 아래 코드를 작성합니다.

```python [실습 2] app/chat_api.py. 카테고리 분류 + 근거 모양 분기
import re

_STRUCTURED_KEYWORDS = [
    "얼마", "몇", "금액", "매출", "수치", "비율", "%", "퍼센트",
    "명", "건", "개수", "총", "평균", "합계", "남은", "남아",
    "언제", "며칠", "시간", "분기", "연도", "년도",
]
_STRUCTURED_RE = re.compile(r"\b\d+[\.%가-힣]?")


def _classify_query_category(query: str) -> str:
    # TODO: 질문을 정형(structured) / 비정형(unstructured)로 분류합니다.
    # 1. 숫자 토큰이 있는지
    if _STRUCTURED_RE.search(query):
        return "structured"
    # 2. 정형 키워드가 하나라도 포함되는지
    lower = query.lower()
    for kw in _STRUCTURED_KEYWORDS:
        if kw in lower:
            return "structured"
    # 3. 그 외는 비정형
    return "unstructured"
```

분류 결과는 `ChatResponse.category` 필드로 같이 내려보냅니다. 그리고 기존 엔드포인트 로직에서:
- `category == "structured"` 이면 DB 조회 결과를 `evidence_json` 필드에 담는다
- `category == "unstructured"` 이고 아직 `evidence_images`가 비어 있으면, 상위 문서의 (source, page)로 `render_evidence_image()`를 불러 이미지를 첨부한다

두 갈래가 한 엔드포인트 안에서 자연스럽게 갈라집니다.

<div style="background:var(--color-surface);border:1px solid var(--color-border);border-radius:var(--radius-lg);padding:24px;margin:var(--space-xl) 0">
<div style="font-size:var(--fs-sm);font-weight:700;color:var(--color-accent-text);margin-bottom:16px;text-align:center">그림 11-4. 카테고리에 따라 갈라지는 근거 모양</div>
<div style="display:grid;grid-template-columns:1fr 1fr;gap:14px">

<div style="border:1px solid var(--color-info);border-radius:8px;padding:12px;background:var(--color-info-bg)">
<div style="font-size:11px;font-weight:700;color:var(--color-info-text);letter-spacing:1px;margin-bottom:8px">정형 질문 → JSON</div>
<div style="font-size:13px;color:var(--color-text);margin-bottom:8px"><b>Q.</b> 영업부 11월 매출 얼마야?</div>
<div style="font-size:12px;color:var(--color-text);padding:8px;background:white;border-radius:4px;margin-bottom:8px">영업부 11월 매출은 <b>1,240,500,000원</b>입니다.</div>
<pre style="background:#fafbfc;border:1px solid var(--color-border);padding:8px 10px;border-radius:4px;font-size:11px;color:var(--color-text-heading);overflow-x:auto;margin:0"><code>{
  "get_sales_sum": {
    "team": "영업부",
    "month": "2025-11",
    "total": 1240500000
  }
}</code></pre>
</div>

<div style="border:1px solid var(--color-accent-warm);border-radius:8px;padding:12px;background:#fff7ed">
<div style="font-size:11px;font-weight:700;color:var(--color-accent-warm-text);letter-spacing:1px;margin-bottom:8px">비정형 질문 → 이미지</div>
<div style="font-size:13px;color:var(--color-text);margin-bottom:8px"><b>Q.</b> 병가 신청 절차 알려줘</div>
<div style="font-size:12px;color:var(--color-text);padding:8px;background:white;border-radius:4px;margin-bottom:8px">병가는 진단서 제출 후 인사팀에 신청합니다...</div>
<div style="height:90px;background:linear-gradient(180deg,#ffffff 0%,#f1f5f9 100%);border:1px solid var(--color-border);border-radius:4px;display:flex;align-items:center;justify-content:center;font-size:11px;color:var(--color-text-subtle)">[PDF 캡처] HR_취업규칙 p.3</div>
</div>

</div>
<div style="margin-top:14px;font-size:12px;color:var(--color-text-muted);line-height:1.6;text-align:center">같은 UI에서 질문의 성격에 따라 근거 모양이 자동으로 바뀝니다. 숫자는 숫자로, 문장은 이미지로</div>
</div>

**동료**: "이제 매출은 매출답게, 규정은 규정답게 보여지네요."

**팀장**: "좋아요. 이걸로 직원들이 '믿고 쓸 수 있는' 에이전트가 됐어요."

*근거의 모양이 곧 답변의 신뢰다.*

## 11.5 마지막 여정: 커넥트HR 에이전트의 완성

<div style="background:var(--color-surface);border:1px solid var(--color-border);border-radius:var(--radius-lg);padding:32px 28px;margin:var(--space-xl) 0">
<div style="font-size:12px;font-weight:700;color:var(--color-text-muted);letter-spacing:1px;margin-bottom:24px;text-align:center">그림 11-5. 완성된 커넥트HR 에이전트 파이프라인. 챕터 4~10의 모든 단계가 조립됐습니다</div>
<div style="display:flex;flex-direction:column;align-items:center;gap:0">
<div style="display:flex;align-items:center;gap:14px;width:100%;max-width:520px">
<div style="flex-shrink:0;width:52px;height:28px;background:var(--color-accent-warm);border-radius:14px;display:flex;align-items:center;justify-content:center;font-size:11px;font-weight:700;color:white">챕터 10</div>
<div style="flex:1;background:#fff7ed;border:2px solid var(--color-accent-warm);border-radius:var(--radius-lg);padding:14px 18px">
<div style="font-size:14px;font-weight:700;color:var(--color-accent-warm-text)">1. 문서 파싱</div>
<div style="font-size:12px;color:var(--color-text-muted);margin-top:2px"><code style="font-size:11px;background:#fff0e6;padding:1px 5px;border-radius:3px;color:var(--color-accent-warm-text)">parse_pdf_hybrid</code> · 스캔이면 Vision, 아니면 pypdf</div>
</div>
</div>
<div style="width:2px;height:20px;background:var(--color-accent)"></div>
<div style="font-size:11px;color:var(--color-accent)">&#9660;</div>
<div style="width:2px;height:8px;background:var(--color-accent)"></div>
<div style="display:flex;align-items:center;gap:14px;width:100%;max-width:520px">
<div style="flex-shrink:0;width:52px;height:28px;background:var(--color-info);border-radius:14px;display:flex;align-items:center;justify-content:center;font-size:11px;font-weight:700;color:white">챕터 8</div>
<div style="flex:1;background:var(--color-info-bg);border:2px solid var(--color-info);border-radius:var(--radius-lg);padding:14px 18px">
<div style="font-size:14px;font-weight:700;color:var(--color-info-text)">2. 청킹</div>
<div style="font-size:12px;color:var(--color-text-muted);margin-top:2px"><code style="font-size:11px;background:#e0ecff;padding:1px 5px;border-radius:3px;color:var(--color-info-text)">semantic_chunking</code> · 의미 단위로 자르기</div>
</div>
</div>
<div style="width:2px;height:20px;background:var(--color-accent)"></div>
<div style="font-size:11px;color:var(--color-accent)">&#9660;</div>
<div style="width:2px;height:8px;background:var(--color-accent)"></div>
<div style="display:flex;align-items:center;gap:14px;width:100%;max-width:520px">
<div style="flex-shrink:0;width:52px;height:28px;background:var(--color-info);border-radius:14px;display:flex;align-items:center;justify-content:center;font-size:11px;font-weight:700;color:white">챕터 8</div>
<div style="flex:1;background:var(--color-info-bg);border:2px solid var(--color-info);border-radius:var(--radius-lg);padding:14px 18px">
<div style="font-size:14px;font-weight:700;color:var(--color-info-text)">3. 검색</div>
<div style="font-size:12px;color:var(--color-text-muted);margin-top:2px"><code style="font-size:11px;background:#e0ecff;padding:1px 5px;border-radius:3px;color:var(--color-info-text)">EnsembleRetriever</code> · Vector + BM25</div>
</div>
</div>
<div style="width:2px;height:20px;background:var(--color-accent)"></div>
<div style="font-size:11px;color:var(--color-accent)">&#9660;</div>
<div style="width:2px;height:8px;background:var(--color-accent)"></div>
<div style="display:flex;align-items:center;gap:14px;width:100%;max-width:520px">
<div style="flex-shrink:0;width:52px;height:28px;background:var(--color-info);border-radius:14px;display:flex;align-items:center;justify-content:center;font-size:11px;font-weight:700;color:white">챕터 8</div>
<div style="flex:1;background:var(--color-info-bg);border:2px solid var(--color-info);border-radius:var(--radius-lg);padding:14px 18px">
<div style="font-size:14px;font-weight:700;color:var(--color-info-text)">4. 리랭킹</div>
<div style="font-size:12px;color:var(--color-text-muted);margin-top:2px"><code style="font-size:11px;background:#e0ecff;padding:1px 5px;border-radius:3px;color:var(--color-info-text)">CrossEncoderReranker</code> · 면접관 재확인</div>
</div>
</div>
<div style="width:2px;height:20px;background:var(--color-accent)"></div>
<div style="font-size:11px;color:var(--color-accent)">&#9660;</div>
<div style="width:2px;height:8px;background:var(--color-accent)"></div>
<div style="display:flex;align-items:center;gap:14px;width:100%;max-width:520px">
<div style="flex-shrink:0;width:52px;height:28px;background:var(--color-success);border-radius:14px;display:flex;align-items:center;justify-content:center;font-size:11px;font-weight:700;color:white">챕터 9</div>
<div style="flex:1;background:var(--color-success-bg);border:2px solid var(--color-success);border-radius:var(--radius-lg);padding:14px 18px">
<div style="font-size:14px;font-weight:700;color:var(--color-success-text)">5. 쿼리 재작성</div>
<div style="font-size:12px;color:var(--color-text-muted);margin-top:2px"><code style="font-size:11px;background:#d6f5e0;padding:1px 5px;border-radius:3px;color:var(--color-success-text)">expand_abbreviations</code> · WFH → 재택근무</div>
</div>
</div>
<div style="width:2px;height:20px;background:var(--color-accent)"></div>
<div style="font-size:11px;color:var(--color-accent)">&#9660;</div>
<div style="width:2px;height:8px;background:var(--color-accent)"></div>
<div style="display:flex;align-items:center;gap:14px;width:100%;max-width:520px">
<div style="flex-shrink:0;width:52px;height:28px;background:var(--color-accent);border-radius:14px;display:flex;align-items:center;justify-content:center;font-size:11px;font-weight:700;color:white">LLM</div>
<div style="flex:1;background:var(--color-accent-bg);border:2px solid var(--color-accent);border-radius:var(--radius-lg);padding:14px 18px">
<div style="font-size:14px;font-weight:700;color:var(--color-accent-text)">6. LLM 답변</div>
<div style="font-size:12px;color:var(--color-text-muted);margin-top:2px"><code style="font-size:11px;background:#e0e0ff;padding:1px 5px;border-radius:3px;color:var(--color-accent-text)">ConnectHRAgent</code> · 최종 답변 생성</div>
</div>
</div>
</div>
<div style="margin-top:20px;padding-top:14px;border-top:1px solid var(--color-border-dashed);font-size:12px;color:var(--color-text-muted);line-height:1.7;text-align:center">챕터 4~10의 모든 단계가 하나로 이어집니다</div>
</div>
동료들이 오픈이 자리로 하나둘 모였습니다. 누군가 채팅창에 질문을 타이핑하면 엔터 소리가 사무실 한구석에서 또박또박 울렸고, 답변이 뜰 때마다 모니터 옆에서 고개가 끄덕였습니다. 근거 이미지가 같이 올라오는 순간엔 화면을 가리키는 손가락이 잠깐씩 멈춰 섰습니다.

**동료**: "병가·약어·수치·서약서까지 다 잡네요. 스캔본도 Vision이 받쳐 주고요."

챕터 1의 맛보기 RAG에서 출발해 챕터 11까지. 문서 규칙, 벡터화, RAG 엔진, 통합 에이전트, 운영 안정화, 검색 품질, 쿼리 해석, 멀티모달, 평가, 조립과 근거를 모두 경험했습니다. **커넥트HR 에이전트** 한 명이 완성됐습니다.

위쪽 파이프라인이 "문서 하나가 질문을 만날 때까지"의 여정을 단계별로 보여줬다면, 아래 다이어그램은 같은 시스템을 한 걸음 뒤에서 바라본 모습입니다. 사용자부터 LLM, 에이전트 루프와 도구, 저장소까지 한 장에 담았습니다.

<style>
.d1 {
  background: var(--color-surface);
  padding: 48px 40px;
  margin: var(--space-2xl) 0;
  overflow-x: auto;
  border-radius: var(--radius-xl);
}
.d1 .header { text-align: center; margin-bottom: var(--space-3xl); }
.d1 .header .sub {
  font-family: var(--font-mono);
  font-size: var(--fs-xs);
  color: var(--color-text-secondary);
  letter-spacing: 3px;
  text-transform: uppercase;
  margin-bottom: var(--space-xs);
  font-weight: 700;
}
.d1 .header .title {
  font-family: var(--font-body);
  font-weight: 800;
  font-size: var(--fs-4xl);
  color: var(--color-text-heading);
  letter-spacing: -0.5px;
}
.d1 .header .desc {
  font-family: var(--font-body);
  font-weight: 700;
  font-size: var(--fs-md);
  color: var(--color-text-secondary);
  margin-top: var(--space-xs);
}
.d1 .llm { display: flex; justify-content: center; margin-bottom: var(--space-md); }
.d1 .llm .box {
  background: var(--color-bg);
  border: 1px solid var(--color-text-heading);
  padding: var(--space-lg) var(--space-3xl);
  text-align: center;
  position: relative;
  min-width: 320px;
}
.d1 .llm .box::before,
.d1 .llm .box::after {
  content: "";
  position: absolute;
  top: -4px;
  bottom: -4px;
  width: 6px;
  border: 1px solid var(--color-text-heading);
}
.d1 .llm .box::before { left: -4px; border-right: none; }
.d1 .llm .box::after { right: -4px; border-left: none; }
.d1 .llm .box .l {
  font-family: var(--font-mono);
  font-size: var(--fs-xs);
  color: var(--color-text-secondary);
  letter-spacing: 3px;
  text-transform: uppercase;
  font-weight: 700;
}
.d1 .llm .box .n {
  font-family: var(--font-body);
  font-weight: 700;
  font-size: var(--fs-2xl);
  color: var(--color-text-heading);
  margin: 2px 0;
}
.d1 .llm .box .s {
  font-family: var(--font-mono);
  font-size: var(--fs-xs);
  color: var(--color-text-secondary);
}
.d1 .llm .box .models {
  display: flex;
  flex-direction: column;
  gap: 2px;
  margin-top: var(--space-sm);
  padding-top: var(--space-sm);
  border-top: 1px dashed var(--color-border);
}
.d1 .llm .box .m {
  display: grid;
  grid-template-columns: 140px 1fr;
  gap: var(--space-md);
  align-items: baseline;
  font-family: var(--font-mono);
  font-size: var(--fs-sm);
  text-align: left;
}
.d1 .llm .box .m .mn { color: var(--color-text-heading); font-weight: 700; }
.d1 .llm .box .m .mu { color: var(--color-text-secondary); font-size: var(--fs-xs); }
.d1 .llm .box .m .ch {
  display: inline-block;
  background: var(--color-accent-bg);
  color: var(--color-accent-text);
  font-size: 10px;
  padding: 1px 6px;
  border-radius: 3px;
  font-weight: 700;
  margin-right: 4px;
}
.d1 .llm-flow {
  display: flex;
  justify-content: center;
  align-items: center;
  gap: 40px;
  margin: var(--space-md) 0 var(--space-2xl);
  font-family: var(--font-mono);
  font-size: var(--fs-xs);
  color: var(--color-text-secondary);
  letter-spacing: 1.5px;
  text-transform: uppercase;
}
.d1 .llm-flow .u { color: var(--color-accent); font-weight: 700; }
.d1 .llm-flow .d { color: var(--color-success); font-weight: 700; }
.d1 .grid {
  display: grid;
  grid-template-columns: 180px 36px 1fr 36px 260px;
  gap: var(--space-sm);
  align-items: stretch;
  min-width: 980px;
}
.d1 .flow-arrows {
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
  gap: var(--space-md);
  font-family: var(--font-mono);
  letter-spacing: 1px;
  text-transform: uppercase;
}
.d1 .flow-arrows .fa-pair { display: flex; flex-direction: column; align-items: center; gap: 1px; }
.d1 .flow-arrows .fa-label {
  font-size: 8px;
  color: var(--color-text-subtle);
  font-weight: 700;
  letter-spacing: 1px;
  white-space: nowrap;
}
.d1 .flow-arrows .fa-arrow { font-size: var(--fs-lg); font-weight: 700; line-height: 1; }
.d1 .flow-arrows .fa-arrow.call { color: var(--color-accent); }
.d1 .flow-arrows .fa-arrow.ret { color: var(--color-success); }
.d1 .col-c {
  background: var(--color-bg);
  border: 1px solid var(--color-border);
  padding: var(--space-lg);
  display: flex;
  flex-direction: column;
  justify-content: space-between;
  min-height: 380px;
}
.d1 .col-c .user {
  text-align: center;
  padding: var(--space-lg) var(--space-sm);
  border-bottom: 1px dashed var(--color-border);
}
.d1 .col-c .user .ico {
  font-size: var(--fs-2xl);
  color: var(--color-accent-warm);
  margin-bottom: var(--space-xs);
}
.d1 .col-c .user .n {
  font-family: var(--font-body);
  font-weight: 700;
  font-size: var(--fs-2xl);
  color: var(--color-text-heading);
}
.d1 .col-c .user .s {
  font-size: var(--fs-xs);
  color: var(--color-text-secondary);
  margin-top: 2px;
}
.d1 .col-c .flow {
  display: flex;
  justify-content: space-between;
  padding: var(--space-md) var(--space-sm);
  font-family: var(--font-mono);
  font-size: var(--fs-xs);
  text-transform: uppercase;
  letter-spacing: 1px;
}
.d1 .col-c .flow .d { color: var(--color-accent); font-weight: 700; }
.d1 .col-c .flow .u { color: var(--color-success); font-weight: 700; }
.d1 .col-c .api {
  text-align: center;
  padding: var(--space-lg) var(--space-sm);
  border-top: 1px dashed var(--color-border);
}
.d1 .col-c .api .l {
  font-family: var(--font-mono);
  font-size: 11px;
  color: var(--color-text-secondary);
  letter-spacing: 2px;
  text-transform: uppercase;
  font-weight: 700;
  margin-bottom: var(--space-xs);
}
.d1 .col-c .api .n {
  font-size: var(--fs-2xl);
  color: var(--color-text-heading);
  font-weight: 700;
  letter-spacing: -0.3px;
}
.d1 .col-c .api .s {
  font-family: var(--font-mono);
  font-size: var(--fs-xs);
  color: var(--color-text-secondary);
  margin-top: 2px;
}
.d1 .col-a {
  background: var(--color-bg);
  border: 1px solid var(--color-text-heading);
  padding: var(--space-2xl) var(--space-xl);
  position: relative;
  min-height: 380px;
}
.d1 .col-a .title {
  position: absolute;
  top: -8px;
  left: var(--space-xl);
  background: var(--color-surface);
  padding: 0 var(--space-md);
  font-family: var(--font-body);
  font-weight: 700;
  font-size: var(--fs-lg);
  color: var(--color-text-heading);
}
.d1 .col-a .ops {
  display: grid;
  grid-template-columns: 1fr 1fr 1fr;
  gap: var(--space-sm);
  margin-bottom: var(--space-lg);
}
.d1 .col-a .chip {
  background: var(--color-surface);
  padding: var(--space-md) var(--space-sm);
  text-align: center;
  border-bottom: 2px solid var(--color-border);
}
.d1 .col-a .chip .l {
  font-family: var(--font-mono);
  font-size: 9px;
  color: var(--color-text-subtle);
  letter-spacing: 2px;
  text-transform: uppercase;
  font-weight: 700;
  margin-bottom: 3px;
}
.d1 .col-a .chip .n {
  font-size: var(--fs-md);
  color: var(--color-text-heading);
  font-weight: 700;
}
.d1 .col-a .chip .s {
  font-family: var(--font-mono);
  font-size: 11px;
  color: var(--color-text-secondary);
  margin-top: 1px;
}
.d1 .col-a .core {
  text-align: center;
  padding: var(--space-lg);
  background: var(--color-accent-bg);
}
.d1 .col-a .core .loop-inner {
  background: var(--color-bg);
  padding: var(--space-md);
  margin: var(--space-md) 0;
  border: 1px solid var(--color-accent-border);
  border-radius: var(--radius-md);
}
.d1 .col-a .core .loop-inner .loop-label {
  font-family: var(--font-mono);
  font-size: 9px;
  color: var(--color-accent);
  letter-spacing: 2px;
  text-transform: uppercase;
  font-weight: 700;
  margin-bottom: var(--space-sm);
}
.d1 .col-a .core .loop-inner .stages {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: var(--space-xs);
}
.d1 .col-a .core .loop-inner .st {
  font-family: var(--font-mono);
  font-size: var(--fs-xs);
  padding: var(--space-xs) var(--space-md);
  background: var(--color-surface);
  border: 1px solid var(--color-border);
  letter-spacing: 1px;
  text-transform: uppercase;
  font-weight: 700;
  color: var(--color-text-heading);
}
.d1 .col-a .core .loop-inner .arr {
  color: var(--color-text-subtle);
  font-size: var(--fs-md);
}
.d1 .col-a .core .l {
  font-family: var(--font-mono);
  font-size: 11px;
  color: var(--color-accent);
  letter-spacing: 2px;
  text-transform: uppercase;
  font-weight: 700;
}
.d1 .col-a .core .n {
  font-family: var(--font-body);
  font-weight: 700;
  font-size: var(--fs-2xl);
  color: var(--color-text-heading);
  margin-top: 2px;
}
.d1 .col-a .core .s {
  font-family: var(--font-mono);
  font-size: var(--fs-xs);
  color: var(--color-text-secondary);
  margin-top: 2px;
}
.d1 .col-t {
  background: var(--color-bg);
  border: 1px solid var(--color-border);
  padding: var(--space-2xl) var(--space-lg) var(--space-lg);
  position: relative;
  min-height: 380px;
}
.d1 .col-t .title {
  position: absolute;
  top: -8px;
  left: var(--space-xl);
  background: var(--color-surface);
  padding: 0 var(--space-md);
  font-family: var(--font-body);
  font-weight: 700;
  font-size: var(--fs-lg);
  color: var(--color-text-heading);
}
.d1 .col-t .tool {
  display: grid;
  grid-template-columns: auto 1fr auto;
  gap: var(--space-md);
  align-items: center;
  padding: var(--space-md) var(--space-xs);
  border-bottom: 1px solid var(--color-border-dashed);
}
.d1 .col-t .tool:last-child { border-bottom: none; }
.d1 .col-t .tool .no {
  font-family: var(--font-mono);
  font-size: var(--fs-xs);
  color: var(--color-text-subtle);
  letter-spacing: 1px;
  font-weight: 700;
}
.d1 .col-t .tool .n {
  font-family: var(--font-mono);
  font-size: var(--fs-md);
  color: var(--color-text-heading);
  font-weight: 700;
}
.d1 .col-t .tool .s {
  font-size: var(--fs-xs);
  color: var(--color-text-secondary);
  margin-top: 1px;
}
.d1 .col-t .tool .b {
  font-family: var(--font-mono);
  font-size: 11px;
  color: var(--color-accent-warm-text);
  letter-spacing: 2px;
  text-transform: uppercase;
  font-weight: 700;
  background: #fff4ed;
  padding: 2px var(--space-sm);
  border-radius: var(--radius-sm);
}
.d1 .col-t .tool .b.rag {
  color: var(--color-accent-text);
  background: var(--color-accent-bg);
}
.d1 .stores {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: var(--space-lg);
  margin-top: var(--space-3xl);
  padding-top: var(--space-xl);
  border-top: 1px solid var(--color-border);
}
.d1 .stores .st {
  text-align: center;
  padding: var(--space-lg);
  border: 1px solid var(--color-border);
  background: var(--color-bg);
}
.d1 .stores .st .l {
  font-family: var(--font-mono);
  font-size: 11px;
  color: var(--color-text-secondary);
  letter-spacing: 2px;
  text-transform: uppercase;
  font-weight: 700;
}
.d1 .stores .st .n {
  font-family: var(--font-body);
  font-weight: 700;
  font-size: var(--fs-2xl);
  color: var(--color-text-heading);
  margin: 3px 0;
}
.d1 .stores .st .s {
  font-family: var(--font-mono);
  font-size: var(--fs-xs);
  color: var(--color-text-secondary);
}
.d1 .caption {
  margin-top: var(--space-2xl);
  font-family: var(--font-body);
  font-weight: 700;
  font-size: var(--fs-base);
  color: var(--color-text-secondary);
  text-align: center;
  max-width: 640px;
  margin-left: auto;
  margin-right: auto;
  line-height: 1.7;
}
</style>

<div class="d1">
<div class="header">
<div class="sub">시스템 아키텍처 (System Architecture)</div>
<div class="title">그림 11-6. 커넥트HR 에이전트</div>
<div class="desc">11개 챕터에 걸쳐 완성한 RAG 시스템</div>
</div>

<div class="llm">
<div class="box">
<div class="l">외부 · LLM (External)</div>
<div class="n">Ollama</div>
<div class="models">
<div class="m"><span class="mn">deepseek-r1:8b</span><span class="mu"><span class="ch">챕터 5</span>추론 · Q&amp;A</span></div>
<div class="m"><span class="mn">llama3.1:8b</span><span class="mu"><span class="ch">챕터 6~</span>Tool Calling · 에이전트</span></div>
<div class="m"><span class="mn">qwen2.5vl:7b</span><span class="mu"><span class="ch">챕터 10</span>Vision · 이미지·스캔 파싱</span></div>
</div>
</div>
</div>
<div class="llm-flow">
<span class="u">↑ 프롬프트 (Prompt)</span>
<span class="d">↓ 응답 (Answer)</span>
</div>

<div class="grid">

<div class="col-c">
<div class="user">
<div class="ico">◉</div>
<div class="n">사용자</div>
<div class="s">채팅으로 질문</div>
</div>
<div class="flow">
<span class="d">↓ 질문</span>
<span class="u">↑ 답변</span>
</div>
<div class="api">
<div class="l">API 게이트웨이 (Gateway)</div>
<div class="n">FastAPI</div>
<div class="s">REST · 세션</div>
</div>
</div>

<div class="flow-arrows">
<div class="fa-pair">
<div class="fa-label">요청</div>
<div class="fa-arrow call">→</div>
</div>
<div class="fa-pair">
<div class="fa-arrow ret">←</div>
<div class="fa-label">응답</div>
</div>
</div>

<div class="col-a">
<div class="title">통합 에이전트 · 운영 (Runtime)</div>
<div class="ops">
<div class="chip"><div class="l">캐시 (Cache)</div><div class="n">Response</div><div class="s">TTL 1시간</div></div>
<div class="chip"><div class="l">관찰 (Monitor)</div><div class="n">Tracker</div><div class="s">토큰·로그</div></div>
<div class="chip"><div class="l">재시도 (Resilience)</div><div class="n">Retry</div><div class="s">3회</div></div>
</div>
<div class="core">
<div class="l">실행기 (Executor)</div>
<div class="n">통합 에이전트</div>
<div class="loop-inner">
<div class="loop-label">ReAct 루프 (Loop)</div>
<div class="stages">
<div class="st">Reason</div>
<div class="arr">→</div>
<div class="st">Act</div>
<div class="arr">→</div>
<div class="st">Observe</div>
<div class="arr">↻</div>
</div>
</div>
<div class="s">IntegratedAgent · ReAct 패턴 기반 · 에이전트 + 도구</div>
</div>
</div>

<div class="flow-arrows">
<div class="fa-pair">
<div class="fa-label">도구 호출</div>
<div class="fa-arrow call">→</div>
</div>
<div class="fa-pair">
<div class="fa-arrow ret">←</div>
<div class="fa-label">결과 반환</div>
</div>
</div>

<div class="col-t">
<div class="title">도구 호출 · MCP Tools</div>
<div class="tool">
<div class="no">01</div>
<div><div class="n">leave_balance</div><div class="s">직원 연차 조회</div></div>
<div class="b">SQL</div>
</div>
<div class="tool">
<div class="no">02</div>
<div><div class="n">sales_sum</div><div class="s">부서·기간 매출</div></div>
<div class="b">SQL</div>
</div>
<div class="tool">
<div class="no">03</div>
<div><div class="n">list_employees</div><div class="s">이름·부서 조회</div></div>
<div class="b">SQL</div>
</div>
<div class="tool">
<div class="no">04</div>
<div><div class="n">search_documents</div><div class="s">LCEL · Top-k</div></div>
<div class="b rag">RAG</div>
</div>
</div>

</div>

<div class="stores">
<div class="st">
<div class="l">벡터 저장소 (Vector Store)</div>
<div class="n">ChromaDB</div>
<div class="s">768차원 · 메타데이터</div>
</div>
<div class="st">
<div class="l">관계형 DB (Relational)</div>
<div class="n">PostgreSQL</div>
<div class="s">직원 · 연차 · 매출</div>
</div>
</div>

<div class="caption">
사용자 질문이 FastAPI를 거쳐 통합 에이전트로 들어오고, 운영 레이어 뒤의 ReAct 패턴(Reason · Act · Observe)이 4개 도구를 선택적으로 호출합니다. 상단 Ollama가 답변을 만들어 되돌려 줍니다.
</div>
</div>

## 11.6 전체 구성도의 완성

앞에서 본 아키텍처 다이어그램이 "시스템이 어떻게 생겼는가"였다면, 여기서는 같은 그림을 "어떤 챕터에서 무엇을 채웠는가"로 다시 읽습니다.

<div class="d1">
<div class="header">
<div class="sub">시스템 아키텍처 (System Architecture)</div>
<div class="title">그림 11-7. 전체 구성도의 완성</div>
<div class="desc">챕터 1~11에서 하나씩 채워 넣은 박스의 최종 조립도</div>
</div>

<div class="llm">
<div class="box">
<div class="l">외부 · LLM (External)</div>
<div class="n">Ollama</div>
<div class="models">
<div class="m"><span class="mn">deepseek-r1:8b</span><span class="mu"><span class="ch">챕터 5</span>추론 · Q&amp;A</span></div>
<div class="m"><span class="mn">llama3.1:8b</span><span class="mu"><span class="ch">챕터 6~</span>Tool Calling · 에이전트</span></div>
<div class="m"><span class="mn">qwen2.5vl:7b</span><span class="mu"><span class="ch">챕터 10</span>Vision · 이미지·스캔 파싱</span></div>
</div>
</div>
</div>
<div class="llm-flow">
<span class="u">↑ 프롬프트 (Prompt)</span>
<span class="d">↓ 응답 (Answer)</span>
</div>

<div class="grid">

<div class="col-c">
<div class="user">
<div class="ico">◉</div>
<div class="n">사용자</div>
<div class="s">채팅으로 질문</div>
</div>
<div class="flow">
<span class="d">↓ 질문</span>
<span class="u">↑ 답변</span>
</div>
<div class="api">
<div class="l">API 게이트웨이 (Gateway)</div>
<div class="n">FastAPI</div>
<div class="s">REST · 세션</div>
</div>
</div>

<div class="flow-arrows">
<div class="fa-pair">
<div class="fa-label">요청</div>
<div class="fa-arrow call">→</div>
</div>
<div class="fa-pair">
<div class="fa-arrow ret">←</div>
<div class="fa-label">응답</div>
</div>
</div>

<div class="col-a">
<div class="title">통합 에이전트 · 운영 (Runtime)</div>
<div class="ops">
<div class="chip"><div class="l">캐시 (Cache)</div><div class="n">Response</div><div class="s">TTL 1시간</div></div>
<div class="chip"><div class="l">관찰 (Monitor)</div><div class="n">Tracker</div><div class="s">토큰·로그</div></div>
<div class="chip"><div class="l">재시도 (Resilience)</div><div class="n">Retry</div><div class="s">3회</div></div>
</div>
<div class="core">
<div class="l">실행기 (Executor)</div>
<div class="n">통합 에이전트</div>
<div class="loop-inner">
<div class="loop-label">ReAct 루프 (Loop)</div>
<div class="stages">
<div class="st">Reason</div>
<div class="arr">→</div>
<div class="st">Act</div>
<div class="arr">→</div>
<div class="st">Observe</div>
<div class="arr">↻</div>
</div>
</div>
<div class="s">IntegratedAgent · ReAct 패턴 기반 · 에이전트 + 도구</div>
</div>
</div>

<div class="flow-arrows">
<div class="fa-pair">
<div class="fa-label">도구 호출</div>
<div class="fa-arrow call">→</div>
</div>
<div class="fa-pair">
<div class="fa-arrow ret">←</div>
<div class="fa-label">결과 반환</div>
</div>
</div>

<div class="col-t">
<div class="title">도구 호출 · MCP Tools</div>
<div class="tool">
<div class="no">01</div>
<div><div class="n">leave_balance</div><div class="s">직원 연차 조회</div></div>
<div class="b">SQL</div>
</div>
<div class="tool">
<div class="no">02</div>
<div><div class="n">sales_sum</div><div class="s">부서·기간 매출</div></div>
<div class="b">SQL</div>
</div>
<div class="tool">
<div class="no">03</div>
<div><div class="n">list_employees</div><div class="s">이름·부서 조회</div></div>
<div class="b">SQL</div>
</div>
<div class="tool">
<div class="no">04</div>
<div><div class="n">search_documents</div><div class="s">LCEL · Top-k</div></div>
<div class="b rag">RAG</div>
</div>
</div>

</div>

<div class="stores">
<div class="st">
<div class="l">벡터 저장소 (Vector Store)</div>
<div class="n">ChromaDB</div>
<div class="s">768차원 · 메타데이터</div>
</div>
<div class="st">
<div class="l">관계형 DB (Relational)</div>
<div class="n">PostgreSQL</div>
<div class="s">직원 · 연차 · 매출</div>
</div>
</div>

<div class="caption">
챕터 1의 맛보기에서 출발해 11개 챕터 만에 전 구간이 완성됐습니다. 커넥트HR 에이전트가 사내 문서와 DB, 스캔본과 이미지, 복합 질문까지 한 창구로 답합니다.
</div>
</div>

## 용어 정리

| 본문 속 표현 | 진짜 용어 | 정식 정의 |
|-------------|---------|----------|
| "한 줄로 엮은 조립" | **RAG Pipeline** | 파싱→청킹→검색→리랭킹→쿼리 재작성→LLM 답변까지 하나의 데이터 흐름으로 이어붙인 구조 |
| "답변 옆의 증거" | **Evidence / Citation** | 답변 근거가 된 원본 문서 조각. 텍스트 인용·링크·이미지 캡처 등으로 제공 |
| "정형 / 비정형 분기" | **Response Modality Routing** | 질문 성격에 따라 응답 형태(JSON 수치·문서 이미지 등)를 달리 내보내는 로직 |

:::remember
**이것만은 기억하자**

- **조립이 가장 큰 일의 절반입니다.** 챕터 8~10에서 실험한 튜닝 부품을 한 파일에 함수로 모아 `run_pipeline` 하나로 엮으면 파이프라인이 완성됐습니다. 쌓인 부품이 많을수록 조립의 힘이 커집니다.
- **근거는 답변만큼 중요합니다.** 사용자는 답의 내용뿐 아니라 "어디서 나왔는지"로 신뢰를 판단합니다. 정형 수치는 JSON으로, 비정형 내용은 문서 이미지로. 질문의 성격에 맞춰 근거의 모양을 바꿉니다.
- **여기서 끝이 아닙니다.** 평가셋을 늘리고, 사용자 로그로 실패 쿼리를 수집하고, Langfuse 같은 도구로 운영 중 품질을 상시 관측하세요. 커넥트HR 에이전트는 이제 **계속 자라는** 시스템입니다.
:::
