# CH05 v4 polish 의도 적용 계획

> **v4 본문 특징**: baseline에서 "두 개의 클레임" 새 도입 + "폴링/가짜 배달" 두 클레임 구조로 5.1 통째 재구성. polish 5.1 L47 자리는 v4에 해당 본문 없음 → v4 자체 발전으로 흡수됨.

## v4 점검 결과 — polish 의도 자리 매핑

| polish 의도 자리 | 우선순위 | v4 매핑 | 결과 |
|---|---|---|---|
| 5.1 L47 폴링 도입 6단계 + 비유 끼어듦 (高) | 高 | v4 5.1 "두 개의 클레임"으로 재구성, 폴링은 번호 목록 형태 | **해당 없음** (v4 자체 발전) |
| **5.2.1 L143 세 곳 수정 안내** (FIX-11 中) | 中 | v4 L178 "이제 세 곳을 수정해야 합니다. delivery-service에 ...추가하고, orchestrator에 ...추가하고, order-service에 ..." 한 문장에 세 항목 압축 | **적용** — 번호 목록 분리 |
| **5.5.1 L246 챕터 4·5 대조** (FIX-11 中) | 中 | v4 L280 자리 그대로 baseline 형태 | **적용** — 두 문장 분리 |
| **5.6.8 L433 Upgrade 헤더 도입** (FIX-12 中) | 中 | v4 L467 "Nginx가 중간에 있으면..." 두 문장. polish의 "일반 HTTP는 일회성 통신" 도입 문장 미반영 | **적용** — 한 줄 추가 + 단락 분리 |
| 5.2.1 L73 STOMP note 압축 (低) | 低 | 보류 | 생략 |
| 5.6.3~5.6.4 STOMP 비유 부재 (中) | 中 | v4 본문 점검 필요. "의도된 깊이" 가능성 | 생략 |
| 5.7.5 시나리오 캐릭터 부재 (低) | 低 | 보류 | 생략 |
| 코드블록 압축 4건 | — | v4 작가 의도 | 생략 |

**결과: 자동 적용 3개**

---

## 적용 자리 3개

### I-1. 5.2.1 세 곳 수정 안내 → 번호 목록 분리 (v4 L178)

**v4 현재**:
> 이제 세 곳을 수정해야 합니다. delivery-service에 배달 완료 API를 추가하고, orchestrator에 배달 완료(`delivery-completed`) 토픽 처리를 추가하고, order-service에 WebSocket Push를 구현합니다.

**적용 후**:
> 이제 세 곳을 수정합니다.
>
> 1. delivery-service에 배달 완료 API를 추가합니다.
> 2. orchestrator가 `delivery-completed` 토픽을 처리하도록 합니다.
> 3. order-service에 WebSocket Push를 구현합니다.

**의도**: 세 가지 수정 사항을 쉼표 나열 → 번호 목록. 각 항목이 다른 동사("추가", "추가", "구현")라 호흡이 길어지는데 번호로 분리하면 명확.

---

### I-2. 5.5.1 챕터 4·5 대조 두 문장 분리 (v4 L280)

**v4 현재**:
> 챕터 4에서는 배달 생성 성공 시 즉시 주문 완료 명령(`complete-order-command`)을 발행했지만, 챕터 5에서는 배달 완료를 기다리기 위해 아무것도 발행하지 않습니다.

**적용 후**:
> 챕터 4에서는 배달 생성이 성공하면 즉시 주문 완료 명령(`complete-order-command`)을 발행했습니다. 챕터 5에서는 배달 완료를 기다리기 위해 아무것도 발행하지 않습니다.

**의도**: 100자+ 대조 한 문장 → 두 문장. 한 호흡으로 읽으면 인지부하 큰 자리.

---

### I-3. 5.6.8 Upgrade 헤더 도입 한 줄 추가 + 단락 분리 (v4 L467)

**v4 현재**:
> Nginx가 중간에 있으면 WebSocket 연결이 일반 HTTP로 처리되어 끊어집니다. `/api/ws/` 경로에 **Upgrade 헤더** 를 설정하여 "이 연결은 WebSocket이니 끊지 말라"고 알려줘야 합니다.

**적용 후**:
> 일반 HTTP는 요청-응답 한 쌍으로 끝나는 일회성 통신입니다. Nginx가 응답 후 연결을 닫아버리기 때문에, Nginx가 중간에 있으면 WebSocket 연결이 일반 HTTP로 처리되어 끊어집니다.
>
> `/api/ws/` 경로에 **Upgrade 헤더** 를 설정하여 "이 연결은 WebSocket이니 끊지 말라"고 알려줘야 합니다.

**의도**: (1) "왜 일반 HTTP면 끊어지는가" 한 줄 보강 — "일회성 통신" 원인 명시. (2) 인과(HTTP 일회성 → Nginx 닫음 → WebSocket 끊김) 한 단락 / 해결(Upgrade 헤더) 별도 단락으로 분리. WebSocket Upgrade 헤더가 처음 등장하는 자리라 도입 호흡 정돈.

---

## 검증 체크리스트

- [ ] L178 번호 목록 3항목
- [ ] L280 두 문장 분리
- [ ] L467 "일회성 통신" 한 줄 추가 + 단락 분리
- [ ] v4 새 도입부 ("두 개의 클레임") + 5.5.3 토픽 맵 최종 + 5.7.6 전체 흐름 요약 보존
- [ ] HTML 빌드 성공
