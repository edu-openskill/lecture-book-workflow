# CH05 v8 (Kubernetes 네트워킹) 이론·일관성 검수

- **분석일**: 2026-05-08
- **대상**: `chapters/05-Kubernetes-네트워킹-v8.md`
- **사전 컨텍스트**: CH01~CH06 v_latest
- **검수 영역**: K8s 네트워킹 이론 정확성 / 비유 일관성 / 그림·본문·표 일치성 / 톤·문체

## 진행 요약 (2026-05-09 2차 세션 종료 시점)

**완료 33건 (전체)** — A-1, A-2 (CRITICAL) / A-3, A-4, A-5, A-6, A-7, A-8, A-9, A-10, A-11, A-12, B-1, B-2, B-3, B-4, B-5, B-6, C-2, C-3, C-4, D-1, D-2, D-4, D-5, D-6, D-7, D-8, E-9, F-1, F-4, F-8 (MAJOR/MINOR) / D-9 (MINOR, A-8 연계 자동 해소) / C-1 (CRITICAL, Path A 폐기 → 사용자 모델 기반 경로 적용)

**작업 대기 묶음** — 없음

**남은 미결정 0건** — 전 항목 처리 완료
- MAJOR: 없음 (모두 처리)
- MINOR: 없음 (모두 처리)
- MINOR: A-10, B-1, B-4, C-4, D-1, D-6, D-7 (7건)

## 추가 작업 (검수 가이드 외, 사용자 요청으로 진행)

1. **그림 5-21 컨트롤 플레인 레이아웃 개편**
   - EndpointSlice Controller ↔ API Server 위치 스왑 (Controller 위, API Server 아래)
   - Ingress Controller ↔ API Server watch 양방향 — 대각선 V자로 재배치
   - kube-proxy(2차) ↔ API Server watch 양방향 추가 — SVG 마지막 위치(다른 박스 위)
   - 그림 5-21에서만 "kube-proxy (1차)" / "kube-proxy (2차)" 라벨 → "kube-proxy"로 통일 (부연 라벨이 역할 구분 담당)
2. **그림 5-27 API Server·watch 추가** (5-25 패턴 정합)
   - viewBox 245 → 290
   - 클러스터 박스 안 상단에 API Server 박스 추가 ("Pod 목록(EndpointSlice) 보관")
   - API Server ↔ kube-proxy(2차) watch 양방향 (y=88~110, 5-25 패턴)
   - 기존 컨텐츠 g translate(0, 45)로 시프트
   - 캡션: "kube-proxy(2차)가 미리 받아 둔 Pod 목록에서 살아있는 Pod IP를 골라 변환해 Pod로 전달"
3. **본문 L1449 단순화** — "EndpointSlice Controller가 뒤에서 갱신..." 부분 제거 (그림 5-21이 담당)
4. **:::note "비유에서 단순화한 두 지점 - API Server의 매개" 박스 삭제** — 그림 5-21·5-25·5-27 셋이 watch 시각화 담당, redundant

## 결정 톤 정착 사항 (다음 세션 적용)

- **입문자 단순화 정책 일관**: 사실 정정보다 학습 인지 부담을 우선 평가
- **분량/시각 복잡도 회피**: 옵션 비교 시 단순화 안을 권장
- **개념 모델 단순화 허용**: ingress-nginx ClusterIP 우회(A-6) 같은 구현 디테일은 그대로 둠
- **관련 그림에 사실 시각화는 일관되게**: 그림 5-25에 watch 있으면 5-27에도 (5-21·5-25·5-27 watch 시각화 일관)

## 다음 세션 시작 가이드

1. **C-1 Path A 묶음 작업** (4건 일괄) — 본문/표/그림 5-22 옵션 확정 후 진행
2. **A-12 부터 진행** — 미결정 18건을 한 건씩 분석·결정·적용 흐름
3. **빌드 + 시각 검증 흐름 유지** — 그림 SVG 변경 시 빌드 후 크롬 확인

## 진행 체크리스트

### CRITICAL (3건)
- [x] **C-1**. 송장 비유 의미 충돌 (5.2.4 ↔ 5.3.1) — **사용자 모델 기반 경로 적용 완료 (2026-05-09)**. Path A(5.2.4 송장 정의 유지 + 5.3.1 NAT 비유 재구성)는 폐기. 사용자 모델: "송장 = 우체국 시스템에서 출력해 붙이는 바코드 라벨, 받는 곳 주소는 우편 자체". 5.2.4 본문 손질 (라인 757~759) + 5.2.4 K8s 메시지 미세 정정 (라인 853) + 5.3.1 마지막 두 문장 재서술 (라인 1010). 그림 5-19·5-22 그대로. B-2/B-6/F-8 자동 해소.
- [x] **A-1**. `targetPort` 정의 정확화 (라인 304, 343~347) — Path A 적용 완료 (L347 표 두 칸 정정: `Pod(컨테이너)`→`컨테이너`, `port 값과 동일하게 설정`→`생략 시 port와 같은 값`)
- [x] **A-2**. nodePort 할당 범위 보강 (라인 98, 345) — A안 적용 완료 (L98 주석: "(기본 허용 범위 30000~32767)", L345 표 생략시 칸: "30000~32767 중 하나로 자동 할당. 명시 지정도 같은 범위")

### MAJOR (18건)
- [x] **A-3**. kube-proxy(1차) "직접 변환" 표현 (라인 1127, 1130) — C-2안 적용 완료. 표(L1127) "iptables 규칙을 미리 깔아 두고, 그 규칙으로 받는 곳을 바꿔 다음 단계로 전달", 본문(L1130) "kube-proxy가 미리 깔아 둔 iptables 규칙을 따라, 패킷의 받는 곳이 Ingress Controller Pod의 IP로 바뀌어 전달됩니다 / 바뀐 패킷은 ..."
- [x] **A-4**. "같은 노드" 단정 (라인 1130) — C안 적용 완료. 본문 그대로 두고 L1130 다음에 :::note "미니큐브 단일 노드 환경 기준" 박스 추가
- [x] **A-5**. Endpoints/EndpointSlice 표준 변경 미언급 — EndpointSlice 통일안 적용 완료. 본문/표/그림(5-21·5-27)/캡션/:::note/이것만은-기억하자 9곳 일괄 갱신. "Endpoints"→"EndpointSlice", "Endpoint Controller"→"EndpointSlice Controller", 본문에서는 "Pod 목록(EndpointSlice)"으로 한 번 부연 후 "Pod 목록"으로 풀어 사용. 그림 5-27 라벨만 짧게 "Pod 목록".
- [x] **A-6**. ingress-nginx ClusterIP 우회 미고지 (:::note 보강) — D안(그대로 둠) 결정. 사유: 입문자 단순화 정책. ingress-nginx 우회는 사실 오류가 아닌 구현 디테일이고, 운영자가 직접 손댈 일이 거의 없어 본문 노출 불필요. EndpointSlice 통일(A-5)과 같은 추상화 톤 유지.
- [x] **A-7**. 그림 5-21 "노드 외벽" 표현 vs 멀티 노드 — B안 적용. 3곳 단어 교체: L323 그림 5-6 "노드 외벽 진입 자리"→"각 노드의 외부 포트", L935 그림 5-21 "노드 외벽 진입"→"각 노드의 외부 포트", L1126 비유 표 "노드 외벽의 공개 포트"→"각 노드의 공개 포트"
- [x] **A-8**. term-box(라인 998) kube-proxy 의인화 — A안 적용. "받는 주소를 ... 바꿔서 흘려 보냅니다"→"받는 주소가 살아있는 Pod의 주소로 바뀌도록 iptables 규칙을 미리 깔아 두고 갱신합니다". A-3 본문/표와 톤 정합. D-9(term-box vs 본문 정확성 격차)도 자동 해소.
- [x] **A-9**. "kube-proxy 1차/2차" 명명이 공식 용어 아님 — C안(그대로 둠) 결정. 사유: 분량 부담 회피 + 학습자 인지 부담 최소화. 1차/2차는 학습용 라벨로 유지. 학습자가 `kubectl get pods`로 한 개만 보임을 발견할 위험은 수용. A-4 :::note(단일 노드 가정)가 이미 단순화 시그널 역할.
- [x] **A-11**. 그림 5-21 컨트롤 플레인 watch 화살표 누락 — 부분 적용. EndpointSlice Controller ↔ API Server는 양방향(좌측 "갱신" + 우측 "watch", 간격 30px로 분리)으로 보강. kube-proxy(2차) → API Server 곡선 화살표는 미추가(시각 인지 부담 vs 학습 가치 검토 결과 본문 :::note L1497로 충분).
- [x] **A-12**. 그림 5-23/25/27 IP `10.244.0.5` 중복 — 옵션 A 적용 (2026-05-09). 그림 5-27의 Pod 목록 `0.4/0.5/0.6` → `0.6/0.7/0.8`. 선택 Pod B `10.244.0.5` → `10.244.0.7`. Ingress Controller IP `10.244.0.5`(그림 5-23/25)와 충돌 해소.
- [x] **B-2**. "송장 ↔ NAT 변환" 매핑 명사-동사 충돌 — C-1 사용자 모델 적용으로 자연 해소 (2026-05-09). 5.2.4 송장 정의가 "우체국 부여 바코드 라벨"로 정정되어 표 라인 1132의 "송장 ↔ NAT 변환" 매핑이 의미상 정합. 표 행은 그대로 유지.
- [x] **B-3**. "분류 규칙(5.3.2)" vs "주소 사전(5.2.3)" 비유 분기 — 비유 제거로 해소 (2026-05-09). 5.2.3 본문 라인 675의 "물류 센터의 주소 사전" / "이미 가동된 물류 센터(컨트롤러 Pod)" 비유 제거. "라우팅 규칙" / "Ingress Controller"로 IT 명사화. 5.3.2의 "분류 규칙" 비유는 비유 시각화 절이라 그대로 유지.
- [x] **B-5**. "집배원 ↔ kube-proxy(2차)" 의인화 — A안(자연스러움 유지) 적용으로 자동 해소 (2026-05-09). A-8 처리 시 term-box·본문은 이미 정확하게 정정됨. 비유 표(라인 1452)의 "집배원 ↔ kube-proxy(2차)" 의인화 매핑은 자연스러워 그대로 유지.
- [x] **B-6**. "물류 센터 정보 + 목적지 정보 함께" 모호 — C-1 사용자 모델 적용으로 해소 (2026-05-09). 라인 1010 마지막 두 문장: "새 송장 바코드에는 다음으로 갈 물류 센터의 라우팅 코드, 우편에 적힌 받는 사람·주소는 그대로 따라가며 최종 목적지를 알려 줍니다"로 재서술. NAT가 L7도 새기는 듯한 인상 제거.
- [x] **C-2**. "물류 센터 → 우체국" 흐름의 노드 가정 부정확 — 옵션 C(그대로 둠) 결정 (2026-05-09). 사유: A-4로 추가된 1단계 IT 본문의 :::note "미니큐브 단일 노드 환경 기준" 박스가 이미 단순화 시그널 담당. 추가 본문은 redundant. 입문자 단순화 정책·비유 추상화 허용 정책 정합.
- [x] **C-3**. 집배원이 우편함에 직접 넣는다 — CNI 단계 누락 — 옵션 B(그대로 둠) 결정 (2026-05-09). 사유: 5.3.3 IT 본문이 단축 작업 시 이미 "DNAT까지만" 정확히 명시(라인 1465). CNI는 이 책 범위 밖 다음 레벨 주제. 비유 캡션은 추상화 허용.
- [x] **D-2**. 그림 5-21 Pod A·A / B·B 라벨 중복 — 옵션 A 적용 (2026-05-09). 라인 982·984·988·990: Pod A·A → Pod A1·A2, Pod B·B → Pod B1·B2. 그림 5-1·5-2·5-27의 단일 Pod 라벨과 충돌 없음.
- [x] **D-4**. EndpointSlice Controller가 그림 5-27에 시각화 없음 — 자동 해소 (2026-05-09). 2026-05-08 사용자 추가 작업으로 그림 5-27에 API Server 박스 + watch 양방향 화살표 추가 완료. 캡션도 "kube-proxy(2차)가 미리 받아 둔 Pod 목록..."으로 갱신. EndpointSlice Controller 자체 박스는 그림 5-21이 담당하는 일관 정책 유지.
- [x] **D-5**. 5.1.4 minikube service URL 임시 포트 변동 미설명 — 옵션 C(그대로 둠) 결정 (2026-05-09). 사유: 라인 459(5.2.1 Service의 한계 도입부)에서 "다시 띄울 때마다 포트도 바뀌었습니다"로 이미 포트 변동을 다룸. 학습자에게 자연스러운 위치에서 짚어주는 흐름. 추가 박스 redundant.
- [x] **D-8**. 그림 5-25→5-27 사이 Ingress IP→ClusterIP 변환 명시 누락 — 옵션 E(그대로 둠) 결정 (2026-05-09). 사유: 라인 1317 IT 본문(5.3 단축 작업 결과)이 이미 "해당 Service의 ClusterIP를 다음 목적지로 잡습니다" 명시. 그림 5-25 캡션도 "분류 규칙에서 ClusterIP를 꺼냄"으로 명시. 본문 + 캡션이 이미 변환 사실 담당.
- [x] **E-9**. "송장이 붙는 단계입니다" 비유 단어 끼워넣기 — 자동 해소 (2026-05-09). 5.3 IT 본문 단축 작업 시 라인 1135의 "송장이 붙는 단계입니다. 패킷에 어디로 갈지를 새겨 다음 단계로 넘깁니다." 두 문장 모두 명시적으로 제거됨. 메모리 규칙(`feedback_no_metaphor_alone`, `feedback_no_sem_expression`) 위반 패턴 해소.
- [x] **F-1**. "바코드는 인코딩되어 있어" 부정확 표현 — 옵션 A 적용 (2026-05-09). 라인 756 "인코딩되어" → "구조화되어". 라인 905 표 "인코딩 해석" → "구조 해석". HTTP URL·Host는 평문이라 "인코딩" 단어가 K8s 사실과 어긋남 — "구조화"로 교체해 정합.
- [x] **F-4**. `ingressClassName: nginx` 설명 부족 — 옵션 C(그대로 둠) 결정 (2026-05-09). 사유: 라인 655 YAML 주석 "어느 Controller가 이 규칙을 집행할지 지정"이 이미 핵심 의미 전달. Controller 종류 다양성·K8s 1.18 이전/이후 변화는 내부 디테일로 입문자 학습 부담. 단순화 정책 정합.
- [x] **F-8**. "분류 규칙은 즉시 고쳐 둡니다" 모호 — 옵션 A(삭제) 적용 (2026-05-09). 라인 1199 마지막 문장 "분류 규칙은 우체국 정보가 바뀔 때마다 즉시 고쳐 둡니다." 제거. 갱신 메커니즘은 그림 5-21 watch 화살표 + 본문 후반 :::note(라인 1497)가 담당하므로 redundant. 사용자 단순화 정책 정합.

### MINOR (12건)
- [x] **A-10**. minikube tunnel 설명 보강 (Ingress 80/443 노출) — :::tip 박스 신설로 처리 (2026-05-09). 그림 5-16 캡션(라인 727) 다음에 :::tip "minikube tunnel이 하는 일" 박스 추가. 항목 두 개: LoadBalancer 외부 IP 부여 / Ingress 80·443 호스트 노출. 표(라인 392)는 그대로 유지하고 자세한 내용은 :::tip이 담당.
- [x] **B-1**. "발신자 ↔ 외부 호스트" — 라우팅 단순화 (그대로 둠) (2026-05-09). 검수 노트 자체가 "수정 불필요 가능"으로 결론. 인터넷 라우팅 생략은 분량 고려 OK. 비유 추상화 허용.
- [x] **B-4**. "우체국" 단어가 5.3.1 노드 vs 5.3.3 Service로 다름 — 옵션 B(그대로 둠) 결정 (2026-05-09). 사유: 1단계는 "동네 우체국/우체국 입구", 3단계는 "서울 우체국"으로 이미 수식어 구분 충분. 한국어 문맥에서 동네/서울 차이가 직관 분리.
- [x] **C-4**. 5.2.2 우체국 비유와 5.3 우체국 비유 위계 명시 부족 — 옵션 B(그대로 둠) 결정 (2026-05-09). 사유: 라인 479가 같은 문장에 "Service"와 "우체국" 병기 → 학습자가 자연스럽게 매핑 인식. 5.3.3 비유 표(라인 1466)가 정식 매핑("서울 우체국 = Service") 명시. 5.2.2에 추가 안내는 redundant + 메타 회수.
- [x] **D-1**. Pod IP 대역 불일치 (`10.0.0.x` vs `10.244.0.x`) — 옵션 B(그대로 둠) 결정 (2026-05-09). 사유: 5.1은 selector 개념 설명용 임의 IP, 5.3은 미니큐브 실제 환경 IP. 라인 101 본문이 "IP가 무엇이든 뒤로 묶습니다" 강조라 IP 값 자체는 핵심 아님. 절마다 다른 목적이라 학습 흐름 영향 미미.
- [x] **D-6**. Service vs Ingress 표 비유·IT 표현 혼재 — 옵션 C(그대로 둠) 결정 (2026-05-09). 사유: 표가 5.2.4 송장 비유 본문 직후라 비유 + IT 한 셀 병기가 자연스러움. 분리하면 행 추가로 단순화 톤 위반. 학습자가 비유 회수와 IT 명사 동시에 인식 가능.
- [x] **D-7**. 그림 5-21 캡션 명사구 마침 — 옵션 B(그대로 둠) 결정 (2026-05-09). 사용자 결정: 캡션 라벨 형식 관례로 봐줌. 학습 흐름 영향 미미.
- [x] **D-9**. term-box vs 본문 정확성 격차 (= A-8 연계) — A-8 처리로 자동 해소

---

## CRITICAL

### C-1. 송장 비유 의미 충돌 (5.2.4 ↔ 5.3.1)

- **위치**: 라인 759 (5.2.4) / 라인 1005 (5.3.1)
- **현재**:
  - 5.2.4: "송장에는 두 가지 정보가 함께 있습니다. 위쪽엔 사람이 눈으로 읽는 **받는 곳 주소**(시·동·번지), 아래쪽엔 스캐너로 읽는 **바코드**가 있습니다."
  - 5.3.1: "안쪽 **창구 직원**이 그 외부 주소를 가리고 그 위에 새 **송장**을 붙입니다. 송장에는 바코드가 찍힙니다. 바코드 안에는 우편이 다음으로 갈 **물류 센터의 정보**와 그곳에서 최종 분류될 **목적지 정보**가 함께 들어 있습니다."
- **문제**:
  - 5.2.4 송장 = 받는 곳 주소(L4) + 바코드(L7)가 함께 있는 종이 한 장
  - 5.3.1 송장 = NAT 변환의 결과로 외부 주소 위에 덮어 붙는 새 라벨
  - 같은 단어가 같은 챕터 내 두 의미로 쓰임
  - 더 심각: 5.3.1 본문 "바코드 안에 다음 단계 + 최종 목적지"는 NAT가 마치 L7도 새기는 것처럼 들림 (실제로는 L4만 변환)
- **선택지** (검토 중 폐기됨):
  - ~~**A**. 5.3.1의 "새 송장" → "**새 받는 곳 라벨**(NAT 라벨)"로 단어 교체~~
  - ~~**B**. 5.2.4 송장 정의를 좁혀 "받는 곳 주소만 적힌 라벨"로 재정의~~
  - ~~**C**. 그대로 둠~~

### ★ 저자 결정 — Path A (2026-05-08, 작업 대기)

**핵심 통찰**: "우체국이 받는 곳을 변경한다"는 비유 자체가 부자연스러움 (현실 우체국은 받는 곳 안 바꿈). 또 "편지글까지 읽는다"도 우체국이 봉투 여는 비유라 어색.

**결정 요지**: 5.2.4 송장 비유는 그대로 유지 (현실 택배 + K8s 패킷 구조와 정합). 5.3.1·5.3.2를 재구성해서 NAT를 **"분류해서 다음 단계로 보냄"**으로 표현. 비유는 추상이라 K8s NAT 디테일과 1:1 매핑 안 해도 OK.

**연동 해결**: B-2 (송장↔NAT 매핑 어색), B-6 (바코드 안 두 정보 모호), F-8 (분류 규칙 즉시 갱신 모호)

#### 작업 항목 — 본문·헤딩

| # | 위치 | 현재 | 변경 |
|---|------|------|------|
| 1 | 라인 1002 (5.3 도입부) | "직원이 **송장을 붙입니다**(1단계)" | "직원이 받아서 **다음 단계로 넘깁니다**(1단계)" |
| 2 | 라인 1004 (H4 헤딩) | "5.3.1 1단계 진입 - 우체국 입구에서 **송장을 붙인다**" | "5.3.1 1단계 진입 - 우체국에서 **다음 단계로 분류한다**" |
| 3 | 라인 1005~1006 (5.3.1 비유 본문) | "외부 주소를 가리고 그 위에 새 **송장**을 붙입니다 ..." | 송장은 5.2.4 정의대로 처음부터 있음. "받는 곳 변경" 표현 제거. 핵심 메시지: 송장에는 받는 곳 주소·바코드 함께 인쇄 → 창구 직원이 받아 다음 단계 물류 센터로 넘김 → 자세한 분류는 물류 센터 일 |
| 4 | 라인 1131 (1단계 IT 본문 끝) | "**송장이 붙는 단계**입니다. 패킷에 어디로 갈지를 새겨 다음 단계로 넘깁니다." | 비유 단어 빼고 "이 단계가 외부 진입을 받아 정밀 분류를 위한 다음 단계로 넘기는 역할입니다." |
| 5 | 라인 1187 (5.3.2 시작) | "**송장이 붙은** 우편이 물류 센터에 도착합니다 ... 분류 규칙은 우체국 정보가 바뀔 때마다 즉시 고쳐 둡니다" | "우편이 물류 센터에 도착합니다 ... 분류 규칙은 새 우체국이 등록되거나 사라질 때마다 즉시 갱신합니다" — F-8 함께 해결 |

#### 작업 항목 — 표

| # | 위치 | 변경 |
|---|------|------|
| 6 | 라인 1128 (1단계 비유 표) | "**송장 ↔ NAT 변환**" 행 제거 — B-2 자동 해결 |
| 7 | 라인 1127 | "창구 직원 ↔ kube-proxy(1차)" 설명을 "iptables 규칙으로 패킷을 Ingress Controller Pod로 **분류해 전달**"로 (변환 → 분류) |

#### 작업 항목 — 그림

| # | 위치 | 결정 옵션 |
|---|------|----------|
| 8 | 그림 5-22 (라인 1008~1119) | **결정 보류** — 본문 작업 시 확정. 옵션: A. 단순화(우편 도착→작업대→다음 단계) / B. 제거(권장 — 1단계 가벼워짐) / C. 라벨만 정리(비추) |
| 9 | 라인 1121 (그림 5-22 캡션) | 8번 옵션 따라 변경 |

#### 본문 변경 미리보기

**5.3.1 비유 본문 (라인 1005~1006) 제안**:
> 발신자가 우편을 들고 동네 **우체국** 입구로 들어갑니다. 우편의 **송장**에는 받는 곳 주소와 바코드가 함께 인쇄되어 있습니다. 우체국 안쪽 **창구 직원**이 우편을 받아 작업대로 옮긴 뒤, 다음 단계인 **물류 센터**로 곧장 넘깁니다. 자세한 분류는 물류 센터의 일입니다. 받는 곳 주소만 보면 "우리 동네에서 처리할 우편이 아니다"까지만 알 수 있기 때문입니다.

**1단계 IT 본문 (라인 1130) 제안**:
> 같은 노드의 kube-proxy가 iptables 규칙으로 그 패킷의 받는 곳을 NodePort Service 뒤에 묶인 Ingress Controller Pod의 IP로 변환합니다. 변환된 패킷은 Ingress Controller로 들어갑니다. 이 단계가 외부 진입을 받아 정밀 분류를 위한 다음 단계로 넘기는 역할을 합니다.

(K8s 사실인 NAT는 본문에 그대로 유지. "송장이 붙는" 비유 단어만 제거.)

#### 작업 시 진행 순서

1. 그림 5-22 처리 옵션 확정 (A/B/C)
2. 본문·표·헤딩 텍스트 수정 (1~7번)
3. 그림 5-22 작업 (확정한 옵션대로)
4. HTML 빌드 + 확인
5. 추적 MD 업데이트 (C-1, B-2, B-6, F-8 모두 done 표시)

### A-1. `targetPort` 정의 정확화

- **위치**: 라인 96~98 (YAML 주석), 라인 302~304 (본문), 라인 343~347 (포트 표)
- **현재**:
  - YAML 주석 (라인 97): `targetPort: 80    # Service가 Pod로 요청을 전달하는 포트`
  - 표 (라인 345): `targetPort | Pod(컨테이너) | ... | port 값과 동일하게 설정`
- **문제**:
  - `targetPort`는 Pod 자체의 포트가 아니라 **Pod 안 컨테이너가 listen하는 포트**
  - "`port` 값과 동일하게 설정"은 정확히는 "**생략 시** `port`와 같은 값"
- **선택지**:
  - **A**. 표의 주체를 "Pod(컨테이너)" → "**컨테이너**"로 변경, "생략 시 `port`와 같은 값"으로 명확화 (권장)
  - **B**. A에 더해 본문 라인 304 "셋은 서로 주인이 다른 포트"에 "Pod 안 컨테이너가 listen하는 포트" 명시 보강
  - **C**. 그대로 둠
- **저자 결정**: ___

### A-2. nodePort 할당 범위 보강

- **위치**: 라인 98 (YAML 주석), 라인 345 (포트 표)
- **현재**:
  - YAML 주석: `nodePort: 30080   # 외부에서 노드로 진입할 때 사용하는 포트 (30000~32767)`
  - 표: `생략 시 30000~32767 중 자동 할당`
- **문제**:
  - 30000~32767 범위는 자동 할당뿐 아니라 **명시적 지정 시에도 K8s 기본 제약**
  - kube-apiserver `--service-node-port-range` 옵션 기본값
- **선택지**:
  - **A**. 주석을 "외부에서 노드로 진입할 때 사용하는 포트 (기본 허용 범위 30000~32767)"로 보강
  - **B**. 그대로 둠 (자동 할당 시 범위만 알아도 학습자에겐 충분)
- **저자 결정**: ___

---

## MAJOR

### A-3. kube-proxy(1차) "직접 변환" 표현

- **위치**: 라인 1127 (비유 표), 라인 1130 (1단계 IT 본문)
- **현재**:
  - 표: "창구 직원 — kube-proxy (1차) — iptables 규칙으로 패킷의 받는 곳을 변환해 다음 단계로 전달"
  - 본문: "같은 노드의 kube-proxy가 iptables 규칙으로 그 패킷의 받는 곳을 NodePort Service 뒤에 묶인 Ingress Controller의 IP로 변환합니다."
- **문제**:
  - kube-proxy = **iptables 규칙을 깔아두고 갱신만 하는 데몬**. 패킷 변환은 노드 커널 netfilter
  - 라인 909 표(일하는 위치 = 노드 커널 iptables)와 자기모순
  - "NodePort Service 뒤에 묶인 Ingress Controller"라는 표현도, 정확히는 "ingress-nginx-controller Service의 selector가 가리키는 ingress-nginx-controller Pod"
- **선택지**:
  - **A**. 본문 라인 1130을 "노드 커널이 kube-proxy가 미리 깔아 둔 iptables 규칙에 따라, 패킷의 받는 곳을 ingress-nginx-controller Pod의 IP로 변환합니다"로
  - **B**. 표만 보강 — "iptables 규칙을 깔아두고, 그 규칙으로 받는 곳을 변환"
  - **C**. A + B 모두
- **저자 결정**: ___

### A-4. "같은 노드" 단정

- **위치**: 라인 1130
- **현재**: "**같은 노드**의 kube-proxy가 iptables 규칙으로 ... Ingress Controller의 IP로 변환합니다. 변환된 패킷은 **같은 노드 안** Ingress Controller로 들어갑니다."
- **문제**:
  - 미니큐브 단일 노드에서만 사실. 멀티 노드에선 다른 노드 Pod로 DNAT 후 CNI가 노드 간 전달
  - LoadBalancer 그림(5-5)은 멀티 노드 가정인데 본문은 단일 노드 가정 — 모순
- **선택지**:
  - **A**. "같은 노드 안" 빼고 "(미니큐브 단일 노드 가정)" 명시
  - **B**. "그 노드의 kube-proxy ... 변환된 패킷은 (같은 노드든 다른 노드든) Ingress Controller Pod가 떠 있는 노드로 전달"으로 일반화
  - **C**. 그대로 두고 :::note에 "단일 노드 가정으로 단순화" 한 줄
- **저자 결정**: ___

### A-5. Endpoints/EndpointSlice 표준 변경 미언급

- **위치**: 라인 1297, 1299, 1440, 1467, 1486 등 (Endpoints / Endpoint Controller 사용 전반)
- **현재**: 일관되게 "Endpoints", "Endpoint Controller" 사용
- **문제**:
  - K8s 1.21+ 표준은 EndpointSlice. Endpoints는 레거시
  - "Endpoint Controller"라는 단일 컨트롤러는 더 이상 단순하지 않음 (endpointslice-controller 등 분리)
- **선택지**:
  - **A**. term-box 또는 :::note에 한 줄 — "K8s 1.21 이후 EndpointSlice로 점진 대체되고 있으나, 개념상 역할은 같습니다" (권장 — 학습 단순화 유지)
  - **B**. 본문 전체를 EndpointSlice로 교체 (분량 증가)
  - **C**. 그대로 둠
- **저자 결정**: ___

### A-6. ingress-nginx ClusterIP 우회 미고지

- **위치**: 라인 1302 (5.3.2 본문), 라인 1488~1492 (:::note)
- **현재**: "Ingress Controller → ClusterIP → kube-proxy(2차) → Pod" 흐름을 사실처럼 서술. :::note의 단순화 설명에 API Server 매개만 언급, ClusterIP 우회는 빠짐
- **문제**:
  - ingress-nginx 기본 동작은 Service ClusterIP 우회하고 Pod IP에 직접 라우팅
  - 책의 흐름은 학습용 개념 모델이지만 단순화임을 명시 안 함
- **선택지**:
  - **A**. :::note에 한 줄 추가 — "또한 ingress-nginx는 실제로는 ClusterIP를 거치지 않고 Pod IP에 직접 라우팅하지만, 학습 모델로는 두 단계 흐름이 이해를 돕습니다"
  - **B**. 본문 라인 1443 앞에 "**개념 모델로는**" 추가
  - **C**. A + B 모두
- **저자 결정**: ___

### A-7. 그림 5-21 "노드 외벽" 표현 vs 멀티 노드

- **위치**: 라인 322~323, 935 (그림 5-6 / 5-21)
- **현재**: "노드 외벽 진입 자리", "노드 외벽 진입"
- **문제**:
  - NodePort는 모든 노드의 동일 포트(30080)를 동시에 엶
  - "외벽"은 한 노드에 집중된 외부 포트처럼 들림
  - 5.1.3 그림 5-4(노드 1·2·3 각각 NodePort)와 시각적 모순
- **선택지**:
  - **A**. 1단계 본문에 "(미니큐브에서는 노드가 한 대지만, 실제로는 클러스터의 모든 노드가 30080을 엽니다)" 한 줄 보강
  - **B**. "노드 외벽" → "각 노드의 외부 포트"로 단어 교체
  - **C**. 그대로 둠
- **저자 결정**: ___

### A-8. term-box(라인 998) kube-proxy 의인화

- **위치**: 라인 1003~1005 (term-box)
- **현재**: "kube-proxy: 모든 노드(서버)에 항상 떠 있는 프로그램입니다. 노드로 들어온 요청의 받는 주소를 살아있는 Pod의 주소로 바꿔서 그쪽으로 흘려 보냅니다."
- **문제**:
  - term-box는 책의 정식 정의 위치인데 "직접 패킷 처리"로 단순화
  - 학습자가 "kube-proxy = 패킷 흘려 보내는 프로그램"으로 외울 위험
- **선택지**:
  - **A**. "kube-proxy: 모든 노드에 항상 떠 있는 프로그램입니다. 들어온 요청의 받는 주소가 살아있는 Pod의 주소로 바뀌도록 노드 커널의 iptables/IPVS 규칙을 깔아 두고 갱신합니다." (권장)
  - **B**. 현재 표현 유지하고 끝에 "(실제 패킷 변환은 노드 커널이 이 규칙대로 수행합니다)" 추가
  - **C**. 그대로 둠
- **저자 결정**: ___

### A-9. "kube-proxy 1차/2차" 명명이 공식 용어 아님

- **위치**: 그림 5-21·5-23, 본문 1단계·3단계 전반
- **현재**: 외부 진입(NodePort→Ingress Pod DNAT)과 내부 분배(ClusterIP→Pod DNAT)를 각각 "kube-proxy(1차)"·"kube-proxy(2차)"로 명명
- **문제**:
  - 책 자체 학습용 라벨, K8s 공식 용어 아님
  - 그림 5-21에서 별도 박스로 그려져 "두 개의 kube-proxy가 따로 있다"고 오해 가능
  - 실제로는 같은 kube-proxy 인스턴스가 깔아 둔 같은 iptables 체인이 처리
- **선택지**:
  - **A**. 그림 5-21 캡션·하단에 "(같은 kube-proxy의 두 변환 규칙입니다)" 한 줄
  - **B**. 박스 라벨을 "kube-proxy iptables (외부 진입 규칙)" / "kube-proxy iptables (내부 분배 규칙)"으로 변경
  - **C**. 그대로 둠 (학습용 단순화 유지)
- **저자 결정**: ___

### A-11. 그림 5-21 컨트롤 플레인 watch 화살표 누락

- **위치**: 라인 956~966 (그림 5-21 컨트롤 플레인 섹션)
- **현재**:
  - Ingress Controller ↔ API Server (watch) 양방향
  - Endpoint Controller → API Server (갱신) 단방향
- **문제**:
  - kube-proxy(2차)도 API Server를 watch하는데 시각화 안 됨
  - Endpoint Controller가 어떻게 Pod 변동을 감지하는지(API Server → 양방향 watch) 누락
- **선택지**:
  - **A**. kube-proxy(2차)→API Server watch 화살표 추가 + Endpoint Controller 양방향 watch
  - **B**. A에 더해 캡션 보강 "각 컴포넌트가 API Server를 watch하여 정보를 받습니다"
  - **C**. 그대로 둠 (그림 복잡도 우려)
- **저자 결정**: ___

### A-12. 그림 5-23/25/27 IP `10.244.0.5` 중복

- **위치**: 그림 5-23 라인 1167, 그림 5-25 라인 1322, 그림 5-27 라인 1471~1478
- **현재**:
  - 그림 5-23/25: Ingress Controller Pod IP `10.244.0.5:80`
  - 그림 5-27: 결정된 Pod B IP `10.244.0.5`
- **문제**: 같은 IP가 Ingress Controller Pod와 일반 Pod에 동시 사용 — 학습자 혼동
- **선택지**:
  - **A**. 그림 5-27의 Pod B → `10.244.0.7`, Endpoints 목록도 `10.244.0.6/0.7/0.8`로 분리 (권장)
  - **B**. 그림 5-23/25의 Ingress Controller IP를 `10.244.0.3` 같은 작은 번호로 분리
  - **C**. 그대로 둠
- **저자 결정**: ___

### B-2. "송장 ↔ NAT 변환" 매핑 명사-동사 충돌

- **위치**: 라인 1128 (1단계 비유 표)
- **현재**: "송장 — NAT 변환 — 패킷의 받는 곳을 Ingress Controller의 IP로 변환"
- **문제**:
  - "송장"은 명사, "NAT 변환"은 행위 — 매핑 어색
  - C-1과 연동되어 송장의 의미가 절마다 달라짐
- **선택지**:
  - **A**. 표를 "**새 받는 곳 라벨** — **NAT 변환 결과** — 패킷 받는 곳을 Ingress Controller Pod IP로 변환" (C-1 안 A와 연동)
  - **B**. "송장" 단어 유지하되 "송장(을 붙이는 행위) ↔ NAT 변환"으로 표기 보강
  - **C**. 그대로 둠
- **저자 결정**: ___

### B-3. "분류 규칙(5.3.2)" vs "주소 사전(5.2.3)" 비유 분기

- **위치**: 라인 569 (5.2.3 표) vs 라인 1299 (5.3.2 표)
- **현재**:
  - 5.2.3: "Ingress 리소스 ↔ 주소 사전"
  - 5.3.2: "분류 규칙 ↔ Ingress 규칙"
- **문제**:
  - K8s 공식: "Ingress 규칙(rules)" = Ingress 리소스 안 `spec.rules`
  - 사실상 같은 것을 두 절에서 다른 비유로 부름
- **선택지**:
  - **A**. 5.3.2 표를 "분류 규칙 — **Ingress 리소스(규칙)** — 5.2.3의 주소 사전을 다른 각도에서 본 것"으로 통일 (권장)
  - **B**. "주소 사전"과 "분류 규칙"이 같은 것임을 본문 한 줄 명시
  - **C**. 두 비유 모두 유지 (절마다 다른 각도라는 의도 — 그대로 둠)
- **저자 결정**: ___

### B-5. "집배원 ↔ kube-proxy(2차)" 의인화

- **위치**: 라인 1439 (3단계 비유 표)
- **현재**: "집배원 — kube-proxy (2차) — iptables DNAT으로 ClusterIP를 살아있는 Pod IP로 변환"
- **문제**: A-3·A-8과 동일. 비유는 OK이지만 정식 정의 위치(term-box·본문)가 의인화
- **선택지**:
  - **A**. 비유 표는 유지(자연스러움), term-box·본문만 정확하게 (A-8 안 A 적용 시 자동 해소)
  - **B**. 표 한 줄 설명 보강 — "iptables DNAT 규칙을 갱신해 ClusterIP를 살아있는 Pod IP로 변환되게 함"
  - **C**. 그대로 둠
- **저자 결정**: ___

### B-6. "물류 센터 정보 + 목적지 정보 함께" 모호 (C-1 연계)

- **위치**: 라인 1005
- **현재**: "바코드 안에는 우편이 다음으로 갈 **물류 센터의 정보**와 그곳에서 최종 분류될 **목적지 정보**가 함께 들어 있습니다."
- **문제**:
  - 1단계 NAT는 L4(IP/포트)만 바꿈. L7(URL·Host)은 손대지 않고 그대로 흘러감
  - 본문은 마치 1단계에서 L7도 새로 박는 것처럼 들림
- **선택지**:
  - **A**. C-1 안 A와 연동 — "새 받는 곳 라벨(=NAT 라벨)이 덮여 붙고, 라벨에는 다음 단계 물류 센터 주소가 적힙니다. 우편 안의 원래 바코드(L7)는 손대지 않고 따라갑니다"로 재서술
  - **B**. 그대로 두고 본문에 "(NAT는 L4만 바꿉니다. L7은 그대로 따라갑니다)" 한 줄
  - **C**. 그대로 둠
- **저자 결정**: ___

### C-2. "물류 센터 → 우체국" 흐름의 노드 가정 부정확

- **위치**: 5.3.2 (Ingress Controller=물류 센터), 5.3.3 (Service=서울 우체국)
- **문제**: 비유에서 "동네 우체국 → 물류 센터 → 서울 우체국"이 물리적으로 분리된 거대 시설처럼 그려짐. 미니큐브 단일 노드에선 셋이 같은 노드 안에 있고 kube-proxy iptables가 한 번에 DNAT
- **선택지**:
  - **A**. 본문 라인 1130에 "(미니큐브 단일 노드 환경에서는 우체국 입구와 물류 센터가 같은 건물 안에 있습니다)" 한 줄
  - **B**. 그림 5-21 주석 "1단계 NodePort와 2단계 Ingress Controller가 같은 노드일 수도 다른 노드일 수도 있다"
  - **C**. 그대로 둠 (비유로서의 단순화는 허용)
- **저자 결정**: ___

### C-3. 집배원이 우편함에 직접 넣는다 — CNI 단계 누락

- **위치**: 라인 1355, 1443
- **현재**: "집배원이 ... 우편함에 우편을 넣습니다", "kube-proxy(2차)가 ... 그 IP로 바꿔 보냅니다"
- **문제**: kube-proxy는 DNAT까지만 함. 그 후 Pod로의 실제 전달은 Pod 네트워크(CNI) 일
- **선택지**:
  - **A**. 라인 1443 끝에 "(이후 Pod로의 실제 전달은 노드 간 Pod 네트워크가 처리합니다)" 한 줄
  - **B**. 그대로 둠 (학습 분량 고려)
- **저자 결정**: ___

### D-2. 그림 5-21 Pod A·A / B·B 라벨 중복

- **위치**: 라인 988~991, 994~997
- **현재**: order-service 아래 Pod A · Pod A, stores-service 아래 Pod B · Pod B (각 두 개씩 동일 이름)
- **문제**: "동일 Pod 두 개"로 오해. 보통 Pod A1·A2 또는 A·A'로 표기
- **선택지**:
  - **A**. Pod A1·A2, Pod B1·B2로 변경 (권장)
  - **B**. Pod A·A', Pod B·B'로 변경
  - **C**. 그대로 둠
- **저자 결정**: ___

### D-4. Endpoint Controller가 그림 5-27에 시각화 없음

- **위치**: 라인 1486 (그림 5-27 캡션)
- **현재**: 캡션은 "Endpoint Controller가 Pod IP 목록을 갱신"이라 적었지만 그림에는 Endpoint Controller 박스 없음
- **선택지**:
  - **A**. 그림 5-27에 Endpoint Controller 박스 추가
  - **B**. 캡션을 "Endpoint Controller(그림 5-21 참조)가 Pod IP 목록을 갱신"으로 그림 5-21 참조
  - **C**. 캡션에서 Endpoint Controller 언급 빼고 단순화
- **저자 결정**: ___

### D-5. 5.1.4 minikube service URL 임시 포트 변동 미설명

- **위치**: 라인 442~451
- **현재**: 실행 결과에서 `127.0.0.1:10345 → :3082`로 포트 변경되는데 본문 침묵
- **문제**: 학습자가 "NodePort가 바뀐 건가?" 오해 가능. 실제로는 미니큐브 호스트 측 임시 매핑 포트만 매번 바뀜
- **선택지**:
  - **A**. 라인 449 다음에 "(미니큐브가 호스트로 노출하는 임시 포트는 매번 바뀌지만, 클러스터 안의 NodePort 30080은 그대로입니다)" 한 줄 (권장)
  - **B**. :::note 박스로 자세한 설명
  - **C**. 그대로 둠
- **저자 결정**: ___

### D-8. 그림 5-25→5-27 사이 Ingress IP→ClusterIP 변환 명시 누락

- **위치**: 그림 5-25 (라인 1305~1349), 본문 라인 1302
- **현재**: 그림 5-25는 "결정" 화살표 후 order-service만 표시. Ingress Controller가 패킷의 받는 곳을 ClusterIP로 바꾼다는 변환 단계 명시 없음
- **문제**: 그림 5-25의 도착 패킷은 `10.244.0.5:80`인데 그림 5-27의 도착 패킷은 `10.96.0.20` — 누가 변환했는지 흐름에서 빠짐
- **선택지**:
  - **A**. 그림 5-25에 "Ingress Controller가 패킷 받는 곳을 `10.96.0.20:80`으로 변경" 화살표·라벨 추가
  - **B**. 본문 라인 1302 다음에 "Ingress Controller는 패킷의 받는 곳 IP를 order-service의 ClusterIP인 10.96.0.20으로 바꿔 다음 단계로 전송합니다" 한 줄
  - **C**. A + B 모두
  - **D**. A-6과 함께 처리 — ":::note에 'ClusterIP 단계 단순화' 명시"로 갈음
- **저자 결정**: ___

### E-9. "송장이 붙는 단계입니다" 비유 단어 끼워넣기

- **위치**: 라인 1130 끝
- **현재**: "송장이 붙는 단계입니다. 패킷에 어디로 갈지를 새겨 다음 단계로 넘깁니다."
- **문제**: 메모리 규칙(`feedback_no_metaphor_in_transition.md`) — 비유 단어를 흐름 연결 문장에 끼워넣지 말 것
- **선택지**:
  - **A**. "이 변환이 1단계의 핵심입니다. 패킷의 받는 곳이 바뀌어 다음 단계로 넘어갑니다"로 비유 단어 빼기
  - **B**. 그대로 둠 (비유와 IT 의식적 병행 구조라 허용 범위)
- **저자 결정**: ___

### F-1. "바코드는 인코딩되어 있어" 부정확 표현

- **위치**: 라인 759
- **현재**: "바코드에는 더 자세한 분류 정보(분류 라인·도착 우체국 코드 등)가 **인코딩되어** 있어 스캐너가 있어야 읽힙니다."
- **문제**: HTTP는 평문. URL·Host는 암호화·인코딩 안 됨. 학습자가 "URL·Host가 암호화되어 있나?" 오해 가능
- **선택지**:
  - **A**. "인코딩되어" → "**구조화되어**"로 단어 교체 (권장)
  - **B**. "인코딩되어" → "구조적으로 묶여"로 교체
  - **C**. 그대로 둠
- **저자 결정**: ___

### F-4. `ingressClassName: nginx` 설명 부족

- **위치**: 라인 658 (YAML 주석)
- **현재**: `ingressClassName: nginx           # 어느 Controller가 이 규칙을 집행할지 지정`
- **문제**: Ingress Controller가 여러 종류(nginx, traefik 등) 있을 수 있다는 점, K8s 1.18 이전(`kubernetes.io/ingress.class` 어노테이션) → 이후(`ingressClassName` 필드) 변화 등 누락
- **선택지**:
  - **A**. :::tip 박스 추가 — "ingressClassName은 어느 Ingress Controller(nginx, traefik 등) 한 종류가 이 규칙을 처리할지 고릅니다. 미니큐브 애드온은 nginx 이름으로 등록되어 있어 이 값을 nginx로 지정합니다"
  - **B**. 본문 한 줄 보강 (별도 박스 없이)
  - **C**. 그대로 둠
- **저자 결정**: ___

### F-8. "분류 규칙은 즉시 고쳐 둡니다" 모호

- **위치**: 라인 1188
- **현재**: "분류 규칙은 우체국 정보가 바뀔 때마다 즉시 고쳐 둡니다."
- **문제**: 어떤 정보가 바뀌는지 불명확 (Ingress 규칙? Service IP? Endpoints?). 매핑상 "분류 규칙=Ingress 규칙"이면 사용자가 새 Ingress 리소스 적용 시 갱신
- **선택지**:
  - **A**. "분류 규칙은 (Ingress 리소스를) 새로 등록·수정할 때마다 즉시 갱신됩니다"로 명확화
  - **B**. 비유에서 빼고 본문 IT 설명에서 처리
  - **C**. 그대로 둠
- **저자 결정**: ___

---

## MINOR

### A-10. minikube tunnel 설명 보강

- **위치**: 라인 391~393 (표), 라인 712~728
- **현재**: 표는 "LoadBalancer 타입 서비스에 외부 IP 부여"
- **문제**: minikube tunnel은 Ingress의 80/443도 호스트로 노출
- **선택지**: A. 표 설명에 "+ Ingress의 80/443을 호스트로 노출" 추가  B. 그대로 둠
- **저자 결정**: ___

### B-1. "발신자 ↔ 외부 호스트" 라우팅 단순화

- **위치**: 라인 1125
- **문제**: 비유에서 발신자가 우체국까지 직접 들고 가는 모양. 인터넷 라우팅 생략. 분량 고려 OK
- **선택지**: A. 그대로 둠
- **저자 결정**: ___

### B-4. "우체국" 단어 매핑 분기

- **위치**: 라인 1126 (5.3.1 노드) vs 라인 1438 (5.3.3 Service)
- **문제**: 같은 단어가 단계마다 다른 K8s 객체 가리킴
- **선택지**:
  - **A**. 1단계 = "동네 우체국 = 노드", 3단계 = "지역 우체국 = Service"로 명확히
  - **B**. 그대로 둠 (전후 맥락으로 구분 가능)
- **저자 결정**: ___

### C-4. 5.2.2 우체국 비유와 5.3 우체국 비유 위계

- **위치**: 라인 481 vs 라인 1438
- **선택지**:
  - **A**. 5.2.2에 "(이 우체국이 곧 5.3에서 살펴볼 ClusterIP Service입니다)" 한 줄
  - **B**. 그대로 둠
- **저자 결정**: ___

### D-1. Pod IP 대역 불일치

- **위치**: 그림 5-1 (`10.0.0.x`), 라인 101 vs 그림 5-23/25/27 (`10.244.0.x`)
- **선택지**:
  - **A**. `10.244.0.x`로 통일 (미니큐브 default flannel 대역)
  - **B**. 그대로 둠
- **저자 결정**: ___

### D-6. Service vs Ingress 표(라인 905) 비유·IT 표현 혼재

- **위치**: 라인 905~910
- **현재**: "받는 곳 주소 (IP·포트)" / "송장 바코드 (URL·Host)" — 비유와 IT가 한 셀
- **선택지**:
  - **A**. "보는 정보 (비유)"와 "보는 정보 (IT)" 두 행 분리
  - **B**. 비유 빼고 IT만
  - **C**. 그대로 둠 (5.3 표는 분리, 이 표는 5.2.4 송장 비유 직후라 혼재 자연스러움)
- **저자 결정**: ___

### D-7. 그림 5-21 캡션 명사구 마침

- **위치**: 라인 995
- **현재**: "요청이 Pod까지 닿는 전체 흐름 - 세 단계가 일어나는 위치"
- **선택지**:
  - **A**. "전체 흐름. 세 단계가 일어나는 위치를 보여줍니다"로 ~합니다체
  - **B**. 그대로 둠 (캡션 라벨 형식은 관례)
- **저자 결정**: ___

### D-9. term-box vs 본문 정확성 격차 (A-8 연계)

- **선택지**: A-8 처리 시 자동 해소
- **저자 결정**: A-8과 일괄

---

## 처리 진행 메모

(작업하면서 결정 사항·수정 라인 기록)

| 항목 | 선택 | 결정일 / 작업일 | 비고 |
|------|------|----------------|------|
| C-1 | Path A (5.2.4 유지, 5.3 NAT를 "분류"로) | 결정 2026-05-08 / 작업 대기 | B-2·B-6·F-8 함께 해결. 본문·표 수정 + 그림 5-22 처리 옵션 미확정 |
| B-2 | C-1 Path A에 포함 | 결정 2026-05-08 / 작업 대기 | 라인 1128 "송장↔NAT 변환" 행 제거 |
| B-6 | C-1 Path A에 포함 | 결정 2026-05-08 / 작업 대기 | 라인 1005~1006 본문 재서술 시 자동 해결 |
| F-8 | C-1 Path A에 포함 | 결정 2026-05-08 / 작업 대기 | 라인 1187 "분류 규칙은 새 우체국 등록·삭제 시 즉시 갱신"으로 |
