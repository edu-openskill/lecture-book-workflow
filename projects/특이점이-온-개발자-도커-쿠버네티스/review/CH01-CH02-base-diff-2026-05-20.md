# CH01·CH02 베이스 본문 변경 점검 (HEAD~1 → HEAD)

비교 대상: `chapters/01-왜-컨테이너인가.md`, `chapters/02-Docker-이해하기.md` (둘 다 버전 접미사 없는 베이스)

- HEAD: `a75732c1` "확인중"
- HEAD~1: `f8eb52e1` "챕터 검수"

총 변경량: CH01 ~30줄 삽입·30줄 삭제 / CH02 ~225줄 삽입·175줄 삭제

---

## CH01 — 변경 사항

### 1) 1.2 표준 컨테이너 — 표현 다듬기

| 자리 | 이전 | 변경 후 |
|------|------|---------|
| 첫 문장 | "표준 규격의 상자 **하나**였습니다" | "표준 규격의 상자였습니다" |
| 둘째 문장 | "크레인 **하나가** 상자를 통째로 들어" | "크레인이 상자를 통째로 들어" |
| 셋째 문장 | "세계 물류가 통째로 바뀌었습니다" | "**그렇게** 세계 물류가 통째로 바뀌었습니다" |

수량 강조어 "하나"를 두 곳에서 제거하고, 마지막 문장에 전환어 "그렇게"를 더해 시간 흐름을 분명히 했습니다.

### 2) 1.3 마무리 내면독백 — 비유에서 기술 용어로 회수

원본 한 문장 + 내면독백 교체:

```
이전: 컨테이너 수는 빠르게 늘어납니다. 말콤 맥린의 시대에도 그랬습니다. 수백, 수천 개가 되자…
변경: 컨테이너 수는 빠르게 늘어납니다. 수백, 수천 개가 되자…
```

```
이전 독백: '공장이 생산하고, 관제 센터가 관리한다.'
변경 독백: 'Docker가 컨테이너를 만들고, Kubernetes가 그걸 관리하는구나.'
```

비유 문장("공장·관제 센터") 자리에서 기술 이름(Docker·Kubernetes)을 직접 호명해, 1.3 마무리에서 비유→기술 매핑이 확정되도록 바꿨습니다.

### 3) 1.4 학습 지도 — HTML 카드형에서 SVG 산 등반 그림으로 교체

| 항목 | 이전 | 변경 후 |
|------|------|---------|
| 형식 | `.evolve-flow` 카드 5장(가로 일렬) + 화살표 | inline SVG: 산 능선 + 점선 등반로 + 5개 캠프 카드 |
| 캡션 | (동일) "책 한 권 분량의 학습 여정…" | (동일) |
| 카드 부제 (CH02) | "컨테이너를 띄운다" | "이미지로 격리 공간 만들기" |
| 카드 부제 (CH03) | "여러 개를 한 번에 묶는다" | "Compose로 여러 컨테이너 묶기" |
| 카드 부제 (CH04) | "스스로 살아나게 한다" | "Pod로 자동 복구 만들기" |
| 카드 부제 (CH05) | "주소를 고정해 길을 낸다" | "Service로 주소 고정하기" |
| 카드 부제 (CH06) | "비밀·데이터를 지키고 통합 운영" | "Secret·Volume으로 운영하기" |

카드 부제가 동사형("~한다·~낸다·~묶는다")에서 기술명 + 명사구("Pod로 자동 복구 만들기")로 통일됐습니다.

### 4) 1.4 챕터 요약 5개 불릿 — 종결 톤 통일

| 챕터 | 이전 | 변경 후 |
|------|------|---------|
| 2 | "…직접 이미지 만들어 올려보기." | "…직접 이미지를 만들어 올립니다." |
| 3 | "…Dockerfile과 Docker Compose." | "…Dockerfile과 Docker Compose로 한 번에 띄웁니다." |
| 4 | "…Pod와 Deployment." | "…자동으로 확장하는 Pod와 Deployment를 다룹니다." |
| 5 | "…주소 고정하고 길 내주기." | "…안정적인 주소를 만듭니다." |
| 6 | "…챕터 3에서 만든 서비스를 Kubernetes 위에 얹기." | "…챕터 3에서 만든 서비스를 Kubernetes 위에 올립니다." |

명사형·종결 부재 다섯 항목을 모두 `~합니다·~다룹니다·~만듭니다·~올립니다` 동사 종결로 매듭 지었습니다.

---

## CH02 — 변경 사항

### 1) 2.1.1 끝 내면독백 삭제

```
삭제: *'아! 도커는 내 컴퓨터 OS 하나를 여러 컨테이너가 나눠 쓰면서, 각자 격리된 공간에서 따로 돌게 하는 거구나.'*
```

비유 단락 끝에서 "~구나"로 의미를 한 번 더 정리하던 독백을 들어냈습니다.

### 2) 2.1.2 그림 2-4 (컨테이너 가상화 3계층 SVG) — 커널 박스 추가

호스트 OS 박스 안에 작은 `Kernel` 라벨 박스(`x=440 y=216`)를 신설하고, 그 박스에서 컨테이너 두 개(애플리케이션 계층 상단)로 향하는 점선 화살표 두 줄, "프로세스·메모리·격리 할당" 이탤릭 라벨을 추가했습니다. `viewBox`는 그대로 `0 0 600 320`.

### 3) 2.1.2 본문 — 두 문장 분리, 비유 단락 1개 삭제

```
이전: …Docker 엔진이 그 커널에 "새 격리 공간을 만들어 달라" 고 요청하면, 실제로 프로세스를 격리하는 일은 커널이 합니다.
변경: …Docker 엔진은 "새 격리 공간을 만들어 달라"는 요청만 보냅니다. 실제로 프로세스를 격리하는 일은 커널이 합니다.
```

또한 "VM처럼 OS를 통째로 따로 올리지 않으니 가볍고 빠르게 뜹니다." 절이 위 문장에서 빠졌습니다.

```
삭제 단락: 오픈이는 노트에 "엔진=프런트, 커널=주방장"이라고 한 줄 적었습니다. 적고 나니 컨테이너의 정체와 만들어지는 과정이 머릿속에서 정리됐습니다.
```

### 4) 2.2 본문 — 한 문장 삭제

```
삭제: 로컬에서 만든 이미지를 서버에서 그대로 꺼내 실행하면 서버의 런타임 버전이 뭐든 컨테이너는 자기 이미지 안의 런타임으로 돕니다.
```

### 5) 그림 2-7 (Docker 전체 흐름 SVG) — Docker Hub 그림 위치 조정

```
이전: <g transform="translate(40, 60)">
변경: <g transform="translate(40, 74)">
```

Docker Hub 구름 모양을 14px 아래로 이동.

### 6) 2.2 :::tip 이미지 레이어 — 끝 두 문장 삭제

```
삭제: 여기에 커널은 호스트와 공유하므로 이미지에 담지 않습니다. 이런 구조 덕에 도커 이미지는 OS 전체를 담는 VM 이미지에 비해 훨씬 가볍습니다.
```

### 7) 2.3 Docker 설치 — 단락 재구성 + :::note 신설·기존 박스 삭제

이전: 한 단락 안에 설치 + 버전 확인이 묶여 있었고, 별도 `:::note` "Windows와 macOS의 내부 리눅스"가 본문 아래에 있었음.

변경:
- 첫 문장이 "…설치 파일을 받아 설치합니다."로 끊김
- **NEW** `:::note` 박스 "설치 전 확인 사항" 추가 — Windows BIOS 가상화·WSL2 안내, macOS Apple Silicon·Rosetta 2 안내
- "오픈이는 설치가 끝난 뒤 터미널에 버전을 찍어봤습니다." 단락이 별도로 분리
- 기존 `:::note` "Windows와 macOS의 내부 리눅스" 박스는 삭제

### 8) 2.4.3 끝 내면독백 삭제

```
삭제: *'글자 하나로 포그라운드가 백그라운드가 되네.'*
```

### 9) 2.4.5 자주 쓰는 명령어 표 — Markdown → HTML `<table>`

7행 명령어 표를 HTML로 변환하고, 아래 마무리 단락("조회는 ps, 로그는 logs, 내리기는 stop, 지우기는 rm을 씁니다…") 한 문단을 삭제.

### 10) 2.5 도입 — 도입 다이어그램 신설 + 절 헤더 1개 삭제

이전: `### 2.5.1 네트워크 한눈에 보기` 절 안에서 오픈이의 호기심·"푸드코트" 비유 도입을 두 단락에 풀어 놓았음.

변경:
- 그 절 헤더(`### 2.5.1 네트워크 한눈에 보기`)와 두 단락 삭제
- 자리에 **NEW** SVG 그림 2-12 (외부 브라우저 → 호스트 포트 → iptables(DNAT) → docker0 → web/db 컨테이너 통신 한눈에 보기) 삽입
- 그 아래 짧은 두 단락("컨테이너 네트워크는 세 가지로 정리됩니다…" + "컨테이너끼리 연결하는 방법, 외부 요청…")만 남김

### 11) 2.5 하위 절 번호 시프트

| 이전 | 변경 후 |
|------|---------|
| 2.5.2 컨테이너끼리 — 주방 모니터와 주문 전광판 | 2.5.1 컨테이너 간 통신 |
| 2.5.3 외부에서 들어오기 — 입구 키오스크 | 2.5.2 외부에서 들어오기 — 입구 키오스크 |
| 2.5.4 이름으로 찾기 — 홀의 안내 지도 | 2.5.3 이름으로 찾기 — 홀의 안내 지도 |

소제목 톤도 "주방 모니터와 주문 전광판" → "컨테이너 간 통신"으로 비유 부제를 떼었습니다.

### 12) 2.5.1 본문 — 단락 두 개로 분리 + 메커니즘 묘사 축약

```
이전: 컨테이너가 뜨는 순간 자기만의 Namespace가 새로 생기고 veth pair 한 쌍이 만들어집니다. 한 끝은 컨테이너 안에, 다른 끝은 호스트의 docker0에 자동으로 꽂히면서 docker0가 빈 IP(172.17.0.x)를 컨테이너에 할당합니다. 이때 A 컨테이너가 B의 IP로 호출하면 데이터는 A → docker0 → B의 경로로 흐릅니다. docker0는 어떤 케이블이 어느 IP에 묶여 있는지 표를 들고 있어서 들어온 데이터를 정확히 그 IP의 케이블로만 흘려보냅니다.

변경: 컨테이너가 실행되는 순간 컨테이너의 격리된 네트워크 공간(Namespace)이 케이블(veth pair)을 통해 docker0에 연결됩니다. 그러면 docker0가 빈 IP를 컨테이너에 할당합니다.

이제 A 컨테이너가 B의 IP로 호출하면 데이터는 A → docker0 → B 순서로 흐릅니다.
```

긴 한 문단을 두 단락으로 분리하고, docker0의 패킷 라우팅 묘사 한 문장을 제거.

### 13) 그림 2-13 → 그림 2-14 (컨테이너 네트워크 격리 구조) — 레이아웃 재설계

이전: 호스트 박스가 중앙에만 있고 Namespace A·B는 호스트 박스 바깥(좌·우)에 따로 떠 있던 구도. veth pair는 호스트 박스 안의 `veth-host1·veth-host2` 작은 박스를 거쳐 곡선으로 연결됨.

변경:
- 호스트 박스(`x=20 y=50 w=720 h=200`)를 전체로 확장하고, Namespace A·B를 호스트 박스 안 좌우에 배치
- `veth-host1·veth-host2` 작은 박스 삭제
- veth pair를 양방향 화살표(`marker-start` + `marker-end`) 직선으로 그림 (Namespace A ↔ docker0, docker0 ↔ Namespace B)
- viewBox는 `0 0 760 240` → `0 0 760 260`

그림 번호 2-13 → 2-14 (앞에 신설된 그림 2-12 통신 한눈에 보기 때문에 +1 시프트).

### 14) 그림 2-14 아래 내면독백 — 비유에서 기술 용어로

```
이전: '각자 주방을 따로 쓰면서 케이블 하나로 전광판 본체에 모이는 거네. 격리와 연결이 한 구조 안에 같이 들어 있구나.'
변경: '각 컨테이너는 격리되어 있지만 docker0를 통해 서로 통신하는구나.'
```

### 15) 2.5.2 (외부에서 들어오기) 도입 두 단락 → 한 단락 압축

```
이전 (3단락):
  docker0는 도커가 호스트에 기본으로 만들어 두는 가상 스위치입니다. 이 위에서 동작하는 네트워크를 기본 네트워크라고 부릅니다. 공유기가 집안 기기를 한 네트워크로 묶어주듯…
  그래서 docker run으로 컨테이너를 띄우면 별도 옵션을 지정하지 않는 한 자동으로 이 기본 네트워크에 들어갑니다. 같은 네트워크 안에 있으니 IP만 알면 서로 통신할 수 있습니다.
  문제는 공유기 너머, 즉 호스트 PC 바깥에 있는 외부 사용자입니다. 브라우저처럼 외부에서 컨테이너로 바로 접근하기는 어렵습니다. 이유는 두 가지입니다.

변경 (1단락 + 불릿):
  docker run으로 띄운 컨테이너는 docker0가 만드는 기본 네트워크에 자동으로 들어갑니다. 같은 네트워크 안의 컨테이너끼리는 IP만 알면 통신할 수 있지만, 외부에서는 그 IP로 바로 닿을 수 없습니다. 두 가지 이유가 있습니다.
```

불릿 두 줄(Windows·macOS / 컨테이너 IP)도 표현이 짧아졌습니다:

| 자리 | 이전 | 변경 후 |
|------|------|---------|
| 1번 불릿 | "Windows와 macOS는 리눅스 기반 운영체제가 아니어서 Docker Desktop이 OS 내부에 가상의 리눅스 환경을 만들고 그 안에서 Docker를 실행합니다. docker0와 컨테이너 IP는 모두 이 가상 리눅스 안에 들어 있어서 호스트(Windows·macOS) 터미널에서는 그 IP로 바로 닿을 수 없습니다." | "Windows·macOS는 Docker Desktop 안 가상 리눅스에서 컨테이너가 돌기 때문에, 호스트 터미널에서 그 IP로 직접 접근할 수 없습니다." |
| 2번 불릿 | "설령 리눅스 환경이라 해도 외부 사용자는 컨테이너의 내부 IP를 알 수 없습니다. 컨테이너가 뜰 때마다 IP가 바뀌기도 하고 바깥으로 공개된 주소도 아니기 때문입니다." | "컨테이너 IP는 외부에 공개된 주소가 아니고, 컨테이너가 뜰 때마다 바뀝니다." |

### 16) 포트포워딩 설명 — 사용자 경험 묘사 한 문장 삭제

```
이전: docker run -p 8080:80을 실행하면 Docker는 iptables에 "호스트의 8080 포트로 들어온 요청을 컨테이너의 80 포트로 전달"이라는 규칙을 한 줄 심어둡니다. 이제 브라우저가 localhost:8080을 호출하면 iptables가 이 규칙대로 요청의 도착지를 컨테이너 80 포트로 바꿔치기하고 docker0를 거쳐 해당 컨테이너로 전달합니다. 사용자는 이 변환을 모르고 "localhost에 접속했더니 컨테이너 안의 페이지가 나왔네" 정도로만 경험합니다.

변경: docker run -p 8080:80을 실행하면 Docker는 iptables에 '호스트 8080 → 컨테이너 80' 규칙을 추가합니다. 이후 브라우저에서 8080 포트로 요청이 오면, iptables가 이 규칙대로 도착지를 바꿔 docker0를 거쳐 80 포트를 가진 컨테이너로 전달합니다.
```

규칙 표현이 따옴표 인용에서 축약 화살표로 바뀌고, 마지막 사용자 시점 한 문장이 삭제됐습니다.

### 17) 그림 2-17 → 그림 2-18 (Docker DNS 변환 흐름 SVG) — 사용자 정의 네트워크 점선 박스 신설

- viewBox `0 0 700 220` → `0 0 700 240`
- 그림 안 전체를 감싸는 옅은 인디고 점선 박스(`x=10 y=25 w=680 h=195`) 추가 + 좌상단 라벨 "사용자 정의 네트워크(User-defined Network)"
- app·DNS·db 박스와 화살표·번호 라벨 좌표 모두 +20 y-시프트
- 그림 아래 단락 두 군데에 볼드 강조 추가:
  - "이름과 IP가 자동으로 **Docker DNS**에 등록됩니다"
  - "`db`를 `172.17.0.3`으로 변환"
  - "**IP가 바뀌어도 이름은 그대로**라"

그림 번호는 +1 시프트 (앞에 도입 그림 2-12 신설 영향).

### 18) 2.5.3 끝 내면독백 삭제

```
삭제: *'naver.com처럼 컨테이너도 이름으로 부르면 되는구나. IP는 일일이 외울 필요 없겠네.'*
```

### 19) 2.6 컨테이너 내부 — 그림 2-18 아래 한 문장 축약

```
이전: 프롬프트가 root@...# 모양으로 바뀌었습니다. 방금까지 호스트의 터미널이었는데 이제는 컨테이너 안의 쉘입니다. 호스트와는 분리된 공간에 들어와 있다는 감각이 옵니다.
변경: 프롬프트가 root@...# 모양으로 바뀌었습니다. 방금까지 호스트의 터미널이었는데 이제는 컨테이너 안의 쉘입니다.
```

### 20) 2.6.1 리눅스 명령어 표 — Markdown → HTML `<table>`

12행 명령어 표를 HTML로 변환.

### 21) 2.6.2 도입 단락 — 두 단락 → 한 단락 압축

```
이전 (2단락):
  기본 명령어를 손에 익힌 오픈이는, 앞서 도커 이미지로 받아서 실행했던 nginx를 직접 설치해 보기로 했습니다. 이미지로 받을 땐 한 줄로 끝났던 과정을 손으로 밟아보면, 앞서 배운 이미지가 무엇을 담고 있었는지 역으로 보입니다.
  컨테이너 안은 최소 구성이라 필요한 패키지는 직접 설치해야 합니다. 오픈이는 패키지 목록을 갱신한 뒤 nginx를 설치하고 실행했습니다.

변경 (1단락):
  앞서 이미지로 한 줄에 실행했던 nginx를, 이번엔 컨테이너 안에서 직접 깔아 봅니다. 컨테이너는 최소 구성이라 필요한 패키지는 직접 설치해야 합니다.
```

### 22) nginx 설치 후 단락 — 두 단락으로 분리

```
이전: 설치 로그가 마무리되며 Nginx 설치가 끝났습니다. 오픈이는 설정이 제대로 반영되었는지 확인하기 위해 현재 활성화된 포트를 점검해 보기로 했습니다. net-tools 패키지를 추가로 설치한 뒤, 명령어를 입력해 Nginx가 네트워크상에서 정상적으로 대기 중인지 확인했습니다.

변경: 설치 로그가 마무리되며 Nginx 설치가 끝났습니다.

       오픈이는 네트워크 확인 도구인 net-tools를 추가로 설치하고 Nginx가 어느 포트에서 대기 중인지 확인했습니다.
```

### 23) vim 명령어 표 — Markdown → HTML `<table>`

4행 표(단계·동작·키)를 HTML로 변환. 그 아래 문장 한 군데 다듬기:

```
이전: 오픈이는 감을 잡으려고 간단한 파일을 하나 만들어봤습니다.
변경: 오픈이는 감을 잡기 위해 간단한 파일을 하나 만들어봤습니다.
```

### 24) `find / -name index.html` 실습 + 그림 2-24 삭제

```
삭제 단락: 방금 쓴 문장이 그대로 찍혀 나왔습니다.
          그러고 보니 아까 깔았던 nginx의 기본 페이지는 어디에 놓여 있을까 궁금해졌습니다. 오픈이는 find로 index.html 파일의 위치를 찾아봤습니다.
삭제 코드블록: find / -name index.html
삭제 터미널 캡처: 그림 2-24. 파일 검색 결과
```

이 자리 다음 문장("컨테이너에서 빠져나올 때는 `exit`입니다…")이 바로 이어지면서 자연스럽게 절단됐습니다. 결과적으로 2.6.2의 마지막 그림이 "파일 내용 출력"(이전 2-23, 신규 2-24)로 정리됐습니다.

### 25) 2.6.2 끝 두 단락 — 표현 단순화

```
이전: 내부 구조는 익혔는데 언제 살아 있고 언제 죽는지는 아직 감이 없다는 뜻이었습니다. 컨테이너가 뜻밖에 꺼지면 곤란하니, 오픈이는 이 규칙부터 정리해보기로 했습니다.

변경: 컨테이너가 언제 살아 있고 언제 꺼지는지 아직 헷갈렸습니다. 이 규칙부터 정리해보기로 했습니다.
```

### 26) 2.7.1 끝 — 행동 묘사 다듬기

```
이전: 오픈이는 exit 말고 다른 방식도 있는지 찾아봤습니다. … 같은 방식으로 한 번 더 띄우고 이 조합으로 빠져나왔습니다.
변경: 이번엔 exit 말고 다른 방식을 찾아봤습니다. … 이름을 붙여 한 번 더 실행한 뒤 이 조합으로 빠져나왔습니다.
```

### 27) 2.7.2 — `-d` 옵션 자리, 절 번호 직접 참조 추가 (룰 충돌 후보)

```
이전: 오픈이는 nginx 때 봤던 '-d' 옵션을 사용해 ubuntu 백그라운드로 실행해 보기로 했습니다.
변경: 오픈이는 챕터 2.4.3에서 봤던 -d 옵션을 사용해 ubuntu를 백그라운드로 실행해 보기로 했습니다.
```

**점검 포인트**: 메모리에 `[절·챕터 번호 직접 참조 금지](feedback_no_section_number_refs.md)` 규칙이 있습니다. "본문에 '4.2/4.2.2/3장에서' 금지, '앞에서 본 ~'로 의미 회수." 새로 추가된 "챕터 2.4.3에서"는 이 룰과 충돌하는 자리입니다. 의도를 다시 확인해 보시고, 룰을 유지하실 거면 다음 형태가 후보입니다.

- "앞에서 nginx를 띄울 때 봤던 `-d` 옵션을 사용해…"
- "앞서 nginx에 썼던 `-d` 옵션을…"

### 28) 2.7.2 — 메인 프로세스 도입 단락 압축

```
이전: 실행 중인 프로세스 목록에 ubuntu가 없었습니다. 컨테이너가 실행되자마자 바로 종료되었기 때문입니다. 반면 이전에 실습했던 nginx는 똑같은 -d 옵션으로도 잘 돌아갔습니다.
      오픈이는 그 원인을 찾다 메인 프로세스의 개념을 알게 되었습니다. 도커 컨테이너는 시작과 동시에 실행되는 메인 프로세스가 종료되면 컨테이너도 함께 종료됩니다. 이 관점에서 두 이미지를 비교해 보니 차이가 명확했습니다.

변경: 실행 목록에 ubuntu가 없었습니다. 메인 프로세스가 종료되면 컨테이너도 함께 종료되기 때문입니다. nginx는 똑같은 -d로 잘 돌아갔는데 ubuntu만 안 된 이유도 여기에 있었습니다.
```

### 29) 2.7.2 끝 한 문장 — 옵션 풀이 제거

```
이전: ubuntu를 백그라운드로 살리려면 -dit(-d 백그라운드, -i 사용자와 상호작용, -t 터미널 화면)을 쓰거나 메인 프로세스를 다른 걸로 바꿔야 합니다.
변경: ubuntu를 백그라운드로 실행하려면 -dit을 쓰거나 메인 프로세스를 다른 걸로 바꿔야 합니다.
```

`-d·-i·-t` 각각의 풀이가 사라졌습니다. (이미 앞 절에서 다뤘기에 중복 제거로 보임)

### 30) 2.7.4 소제목 + 도입 단락 정비

| 자리 | 이전 | 변경 후 |
|------|------|---------|
| H3 | `### 2.7.4 attach vs exec` | `### 2.7.4 백그라운드 접속 - attach vs exec` |
| 도입 | "오픈이는 백그라운드로 띄운 컨테이너에 다시 들어가는 방법이 궁금해졌습니다. 알아보니 메인 프로세스에 직접 연결하는 attach, 새 프로세스를 하나 더 띄우는 exec 두 가지 방법을 확인했습니다. 결과는 비슷해 보이지만 내부에서 일어나는 일이 다르다고 해서, 두 가지를 직접 비교해봤습니다." | "백그라운드로 실행한 컨테이너에 다시 들어가는 방법은 두 가지입니다. 메인 프로세스에 직접 연결하는 attach와 새 프로세스를 하나 더 만드는 exec, 결과는 비슷해 보이지만 내부 동작이 다릅니다." |

### 31) 그림 2-29 위 attach 설명 — 위험성 한 문장 위치 이동

```
이전: 그림처럼 attach는 컨테이너 안에서 이미 돌고 있는 메인 프로세스를 연결합니다. 같은 프로세스를 공유하기 때문에 여기서 잘못 빠져나와 프로세스를 종료하면 컨테이너 전체가 같이 꺼집니다.
변경: 그림처럼 attach는 컨테이너 안에서 돌고 있는 메인 프로세스에 그대로 연결합니다.
```

위험성 묘사는 그림 아래 단락("ubuntu의 입력 터미널이 나타났습니다…")으로 이동·축약됐습니다.

### 32) 그림 2-30 아래 단락 — 동일 정보 압축

```
이전: ubuntu의 입력 터미널이 나타났습니다. attach는 메인 프로세스에 직접 연결되므로, 여러 터미널에서 붙어도 모두 같은 화면을 공유합니다. 다만 이 상태에서 잘못 빠져나와 메인 프로세스를 종료하면 컨테이너 전체가 꺼집니다.
변경: ubuntu의 입력 터미널이 나타났습니다. 여러 터미널에서 붙어도 같은 화면을 공유하지만, 잘못 빠져나오면 메인 프로세스가 종료되어 컨테이너 전체가 꺼집니다.
```

### 33) exec 도입 — 명령어 명시

```
이전: 이번에는 같은 컨테이너에 exec로 다시 들어가봤습니다.
변경: 이번에는 같은 컨테이너에 docker exec -it <컨테이너ID> bash로 다시 들어가봤습니다.
```

### 34) exec 실증 단락 — exec 의도 한 줄 다듬기

```
이전: exec가 정말 새 프로세스를 만드는지 눈으로 확인하고 싶어서, 오픈이는 호스트에서 터미널을 하나 더 열고 컨테이너 내부의 프로세스 목록을 찍어봤습니다.
변경: exec가 정말 새 프로세스를 만드는지 눈으로 확인하기 위해 호스트에서 터미널을 하나 더 열고 컨테이너 내부의 프로세스 목록을 찍어봤습니다.
```

### 35) 그림 2-34 아래 설명 — 변수 사례 표현 다듬기

```
이전: 두 프로세스는 같은 컨테이너 안에서 환경을 공유하지만 터미널 세션은 서로 독립입니다. 그래서 exec에서 선언한 임시 변수가 메인 프로세스 세션에서는 보이지 않고, 반대도 마찬가지입니다.
변경: 두 프로세스는 같은 컨테이너의 파일과 네트워크를 공유합니다. 하지만 셸 세션은 독립이라 한쪽에서 만든 변수는 다른 쪽에서 보이지 않습니다.
```

### 36) 2.7.4 끝 — 비교표 삭제 + 내면독백 다듬기

```
삭제: attach vs exec 4행 markdown 비교표 (방식·동작·위험성)
```

```
이전 독백: 'attach는 메인, exec는 새 방. 앞으로는 exec만.'
변경 독백: 'attach는 메인 프로세스에 연결되고, exec는 새 프로세스를 띄우는구나. 앞으로는 exec만 쓰자.'
```

### 37) 2.8 commit — 도입 두 단락 재배치·축약

기존 2.7.4 끝의 "여기까지 공부하니… 컨테이너를 내리는 순간 지금까지 했던 작업이 사라지는 문제였습니다." 단락이 2.8 도입으로 이동하면서 다음과 같이 축약:

```
이전 도입 (3 단락):
  여기까지 공부하니 컨테이너의 생명 주기는 더 이상 헷갈리지 않았습니다. 그런데 한 가지 생각이 오픈이를 붙잡았습니다. 앞서 Ubuntu 컨테이너에 패키지를 설치하고 파일을 추가했는데, 컨테이너를 내리는 순간 지금까지 했던 작업이 사라지는 문제였습니다.
  *'내가 수정한 이 상태를 기록해두려면 어떻게 해야 하지?'*
  필요한 건 지금 컨테이너 상태를 그대로 기록해 두는 방법입니다.
  ## 2.8 commit — 실행 중인 컨테이너를 이미지로
  그 일을 하는 명령이 docker commit입니다. 이 명령어는 컨테이너의 현재 상태를 그대로 이미지로 저장합니다.
  지금까지 오픈이는 남이 올려둔 이미지(ubuntu, nginx, tomcat)를 Docker Hub에서 받아 쓰기만 했습니다. 반대로, 직접 수정한 컨테이너를 이미지로 만들어 Docker Hub에 올릴 수도 있습니다. 그러면 다른 컴퓨터에서도 이 이미지를 받으면 같은 상태로 쓸 수 있습니다.

변경 도입 (2 단락):
  ## 2.8 commit — 실행 중인 컨테이너를 이미지로
  Ubuntu 컨테이너에 패키지를 설치하고 파일을 추가했는데, 컨테이너를 내리는 순간 그 작업이 모두 사라집니다.
  *'내가 수정한 이 상태를 기록해두려면 어떻게 해야 하지?'*
  컨테이너의 현재 상태를 그대로 이미지로 저장하는 명령이 docker commit입니다.
  지금까지는 남이 올려둔 이미지(ubuntu, nginx, tomcat)를 받아 쓰기만 했습니다. 이번엔 반대로 직접 만든 이미지를 Docker Hub에 올려보겠습니다.
```

### 38) 그림 번호 시프트 — 전반적인 +1 (그림 2-12부터)

2.5 도입에 그림 2-12(통신 한눈에 보기)가 신설되고, 2.6.2 끝의 find 결과 그림이 삭제되면서 전체적으로 그림 번호가 한 칸 밀렸습니다.

| 베이스라인 (이전) | 신설/삭제 영향 후 (현재) |
|--------------------|-----------------------|
| — | 그림 2-12 (NEW. 컨테이너 통신 한눈에 보기) |
| 그림 2-12 주방 모니터·전용 통신 케이블·주문 전광판 | 그림 2-13 |
| 그림 2-13 컨테이너 네트워크 격리 구조 | 그림 2-14 |
| 그림 2-14 입구 키오스크 비유 | 그림 2-15 |
| 그림 2-15 호스트 포트가 컨테이너 포트로 | 그림 2-16 |
| 그림 2-16 안내 지도 비유 | 그림 2-17 |
| 그림 2-17 Docker DNS 변환 | 그림 2-18 |
| 그림 2-18 Ubuntu 컨테이너 안 | 그림 2-19 |
| 그림 2-19 nginx 설치 실행 | 그림 2-20 |
| 그림 2-20 포트 상태 확인 | 그림 2-21 |
| 그림 2-21 nginx 환영 페이지 | 그림 2-22 |
| 그림 2-22 vim 편집 | 그림 2-23 |
| 그림 2-23 파일 내용 출력 | 그림 2-24 |
| 그림 2-24 파일 검색 결과 (find) | (삭제) |
| 그림 2-25 이후 | (베이스 그대로 유지. 베이스의 다음 번호 그림이 현재 2-25부터 시작하는지 또는 차이가 0이 되는지는 2.7 이하 미변경 확인 결과 차이 없음) |

2.7 이후의 그림 번호는 2.6.2에서 한 장이 삭제·한 장이 신설되어 정확히 상쇄되므로 베이스와 동일하게 유지됐습니다.

---

## 종합

- **삭제 위주**: 메타·중복 문장 정리. 내면독백 5건(`아!~구나` 류 2건, `글자 하나로…`, `attach는 메인…`, `naver.com처럼…`), 비유 추가 단락 2건(엔진=프런트·커널=주방장 노트, find 실습 그림+단락) 정리
- **신설**: 1.4 SVG 산 등반 그림, 2.1.2 그림 2-4 Kernel 박스, 2.3 설치 전 확인 사항 `:::note`, 2.5 도입 그림 2-12, 2.5.3 사용자 정의 네트워크 점선 박스
- **표 변환**: Markdown 표 3개(2.4.5 자주 쓰는 명령어, 2.6.1 리눅스 명령어, 2.6.2 vim 키)가 HTML `<table>`로
- **이중 톤 통일**: 1.4 챕터 요약 불릿 5개 종결 통일, 2.5 절 소제목 비유 부제 제거
- **재배치**: 2.7.4 끝 commit 도입 단락이 2.8로 이동
- **번호 시프트**: CH02 그림 2-12에서 +1, 2.6.2 한 장 삭제로 -1 → 2.7 이후 변동 없음
- **2.5 절 번호 +1 시프트**: 2.5.2 → 2.5.1, 2.5.3 → 2.5.2, 2.5.4 → 2.5.3 (구 2.5.1 헤더 삭제 영향)

## 점검이 필요한 자리

1. **2.7.2 "챕터 2.4.3에서"** — 메모리 `feedback_no_section_number_refs` 규칙과 충돌. 의도하신 추가라면 룰을 갱신하시거나, 룰 유지면 "앞에서 nginx에 썼던 -d 옵션" 같은 형태로 다시 다듬는 게 자연스럽습니다.
