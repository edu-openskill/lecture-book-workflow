# Ch.1 왜 컨테이너인가

> 한 줄 요약: 컨테이너는 환경을 통째로 포장해서 어디서든 똑같이 실행하는 기술이다
> 핵심 개념: 컨테이너, Docker, Kubernetes

## 1.1 말콤 맥린 : 컨테이너의 아버지

### 1.1.1 해상 운송의 초기

Docker를 이해하려면 '컨테이너'라는 개념이 어디서 왔는지부터 살펴야 합니다. 그 시작은 의외로 바다 위에 있습니다.

컨테이너가 등장하기 전, 해상 물류는 온전히 사람의 힘에 의존했습니다. 항구에 배가 도착하면 수백 명의 노동자가 화물을 하나씩 내리고 분류하고 다시 트럭에 옮겨 실어야 했습니다. 길게는 몇 주씩 걸리는 작업이었습니다.

<!-- image-prompt: Minimal black line drawing on white background, very few details, icon-like simplicity, 4:3 aspect ratio, 800x600px. With Korean labels: An old harbor scene with workers carrying sacks and crates from a cargo ship (labeled "화물선") to the dock (labeled "부두"), showing the inefficient process of early maritime logistics, with a label "수작업 하역" above the workers -->

![](../assets/CH01/chap01-1.png)
*초기 해상 물류의 수작업 하역 과정*

배로 물건을 보내는 비용보다 사람이 짐을 싣고 내리는 인건비가 더 비쌌습니다. 트럭 운전사들도 화물을 기다리느라 항구에서 며칠씩 대기하기 일쑤였습니다.

### 1.1.2 컨테이너의 도입

이 비효율을 가장 가까이에서 체감한 사람이 있었습니다. 바로 트럭 운전사 말콤 맥린입니다.

항구에서 화물이 하나씩 옮겨지는 걸 반복적으로 지켜보던 그는 단순하지만 강력한 의문을 품습니다. **"왜 짐을 통째로 옮기지 않는 걸까?"**

이 질문에서 컨테이너라는 개념이 탄생했습니다. 화물을 낱개로 다루는 대신, 표준 규격의 컨테이너 하나에 담아서 통째로 옮기자는 아이디어였습니다.

크레인으로 컨테이너를 그대로 배에 올리고, 도착지에서도 같은 방식으로 내리면 됩니다. 운송 속도와 효율은 이전과는 비교할 수 없을 정도로 올라갔습니다.

<!-- image-prompt: Minimal black line drawing on white background, very few details, icon-like simplicity, 4:3 aspect ratio, 800x600px. With Korean labels: A modern container port with a large crane (labeled "크레인") lifting a standardized shipping container (labeled "표준 컨테이너") from a truck (labeled "트럭") onto a cargo ship (labeled "화물선"), showing the efficient and automated container logistics system -->

![](../assets/CH01/chap01-2.png)
*표준 컨테이너와 크레인을 활용한 현대 해상 운송*

물론 처음부터 순탄하지는 않았습니다. 기존 선박은 컨테이너를 싣는 걸 전제로 만들어진 것이 아니었기 때문에 공간 활용이 어려워 기대만큼의 성과가 나오지 않았습니다.

맥린은 여기서 멈추지 않았습니다. 아예 컨테이너 전용 선박을 직접 만들어 버립니다.

그 상상은 결국 현실이 되었습니다. 컨테이너는 해상 운송의 표준으로 자리 잡았고 전 세계 물류 체계를 바꿔 놓았습니다.

### 1.1.3 IT에서의 컨테이너

그렇다면 이 이야기가 IT와 무슨 관련이 있을까요? IT에서 말하는 '컨테이너'도 해상 운송의 컨테이너와 본질은 같습니다.

> **컨테이너(Container)** 는 어디서든 동일하게 실행되는 표준화된 실행 환경을 의미합니다.

<!-- image-prompt: Minimal black line drawing on white background, very few details, icon-like simplicity, 4:3 aspect ratio, 800x600px. With Korean labels: A developer frustrated at their desk with two monitors showing different environments - left monitor shows "개발 환경 OK" with green checkmark, right monitor shows "운영 환경 ERROR" with red X mark, illustrating environment mismatch problems -->

![](../assets/CH01/chap01-3.png)
*개발 환경과 운영 환경의 차이로 인한 오류 발생*

개발 환경에서 아무리 열심히 테스트해도 운영 환경에 배포하는 순간 또 다른 변수가 기다리고 있습니다.

컨테이너는 바로 이 문제를 해결합니다. 애플리케이션이 실행되는 데 필요한 라이브러리와 설정 파일, 의존성을 전부 하나의 컨테이너에 담아 버립니다. 말콤 맥린이 화물을 표준 컨테이너에 넣어 어디서든 똑같이 옮길 수 있게 한 것과 같은 원리입니다.

<!-- image-prompt: Minimal black line drawing on white background, very few details, icon-like simplicity, 4:3 aspect ratio, 800x600px. With Korean labels: A shipping container metaphor for software - a standardized box (labeled "컨테이너") containing "애플리케이션 코드", "라이브러리", and "설정 파일", being deployed identically across three targets labeled "개발 노트북", "테스트 서버", and "운영 클라우드 서버" -->

![](../assets/CH01/chap01-4.png)
*컨테이너를 통한 일관된 실행 환경*

## 1.2 Docker와 Kubernetes

컨테이너라는 개념은 이해했습니다. 그런데 개념만으로는 아무것도 실행할 수 없습니다.

맥린도 마찬가지였습니다. "짐을 통째로 옮기자"는 아이디어만으로는 화물이 움직이지 않았습니다. 표준 규격에 맞는 컨테이너를 찍어내는 공장이 필요했고, 컨테이너 전용 선박과 크레인을 갖춘 항구가 필요했습니다. 컨테이너가 수백, 수천 개로 불어나자 어떤 배에 어떤 컨테이너를 싣고, 도착지에서 어떤 순서로 내릴지 자동으로 관리하는 시스템까지 갖춰야 했습니다.

IT도 같은 순서를 밟았습니다.

**Docker** 는 컨테이너를 만들고 실행하는 도구입니다. 애플리케이션과 실행 환경을 하나의 이미지로 묶고, 그 이미지를 컨테이너로 실행합니다. 맥린의 이야기에서 컨테이너를 찍어내는 공장에 해당합니다.

**Kubernetes** 는 컨테이너를 운영하는 플랫폼입니다. 서비스가 성장하면 프론트엔드, 백엔드, 데이터베이스, 캐시 서버까지 컨테이너가 수십, 수백 개로 늘어납니다. 컨테이너가 죽으면 자동으로 살리고, 트래픽이 몰리면 자동으로 늘리고, 새 버전을 중단 없이 배포합니다. 수백 개의 컨테이너를 자동으로 싣고 내리고 관리하던 항구 시스템, 그 역할입니다.

| 구분 | Docker | Kubernetes |
|------|--------|------------|
| 역할 | 컨테이너를 만들고 실행 | 컨테이너 운영 자동화 |
| 비유 | 화물을 컨테이너에 담는 기술 | 컨테이너를 관리하는 항구 시스템 |
| 핵심 기능 | 이미지 빌드, 컨테이너 실행 | 자동 복구, 스케일링, 무중단 배포 |

이 책에서는 Docker를 먼저 익힌 뒤 Kubernetes로 넘어갑니다. 컨테이너를 직접 다뤄본 뒤에 운영 자동화를 배우면, 왜 필요한지가 자연스럽게 체감됩니다.

## 1.3 이 책의 학습 흐름

이 책은 컨테이너 하나를 띄우는 것에서 시작해, Kubernetes 환경에서 여러 컨테이너를 연결하고 관리하는 것까지 단계별로 경험하는 여정입니다. 각 챕터는 이전 챕터의 한계를 느끼는 것에서 시작합니다.

<!-- image-prompt: Minimal black line drawing on white background, 16:9 aspect ratio, 1280x400px. A winding path/road from left to right, like a journey map. Starting point on the far left: a small flag icon labeled "시작". Three stops along the path, each marked with a milestone marker. Stop 1: signpost "챕터 2", below it "Docker 이해하기", a small icon of a single container box. Stop 2: signpost "챕터 3", below it "Docker 다루기", an icon showing multiple containers connected together. Stop 3 (destination): signpost "챕터 4", below it "Kubernetes 시작하기", an icon showing containers with a control tower/crane above them. The path gets slightly wider/bolder as it progresses, suggesting growth. No colors, no fill, just clean black lines. -->

![](../assets/CH01/chap01-roadmap.png)
*이 책의 학습 흐름*

**챕터 2 — Docker 이해하기.** Docker의 작동 원리를 이해하고, 직접 설치해서 사용해 봅니다. 컨테이너 하나를 띄우는 것부터 시작해 리눅스 명령어로 내부를 탐색하고, 이미지를 직접 만들어 Docker Hub에 공유합니다.

**챕터 3 — Docker 다루기.** 챕터 2에서는 컨테이너를 만들 때마다 같은 패키지를 반복 설치해야 했습니다. Dockerfile로 이 과정을 자동화하고, 여러 종류의 컨테이너를 만들어 Docker Compose로 한 번에 띄우고 서로 연결해 봅니다.

![](../assets/CH01/chap01-ch3-arch.png)
*챕터 3에서 다루는 컨테이너 구성*

**챕터 4 — Kubernetes 시작하기.** 여러 컨테이너를 띄울 수 있게 되었지만, 컨테이너가 죽으면 누가 살려주고, 새 버전은 어떻게 중단 없이 배포할까요? 로컬에 Kubernetes 환경을 구성하고, Pod · Deployment · Service로 자동 복구와 무중단 배포를 직접 경험합니다.

![](../assets/CH01/chap01-ch4-arch.png)
*챕터 4에서 다루는 Kubernetes 환경*

**챕터 5 — Kubernetes 운영하기.** 챕터 4에서 핵심 리소스를 익혔다면, 이번에는 실제 서비스를 운영하는 데 필요한 것들을 다룹니다. ConfigMap과 Secret으로 설정을 외부에서 주입하고, Ingress로 외부 요청을 라우팅하며, 챕터 3에서 만든 여러 컨테이너를 Kubernetes 위에 배포합니다.

## 이것만은 기억하자

- **컨테이너에 담으면 어디서든 똑같다.** 말콤 맥린이 화물을 표준 컨테이너에 넣어 전 세계 어디든 보낸 것처럼, Docker는 애플리케이션을 컨테이너에 담아 어디서든 동일하게 실행합니다.
- **Docker는 컨테이너를 만들고, Kubernetes는 컨테이너를 운영한다.** Docker가 컨테이너를 만드는 기술이라면, Kubernetes는 수백 개의 컨테이너를 자동으로 관리하는 항구 시스템입니다.
- **이 책은 컨테이너 하나에서 시작해 Kubernetes까지 도달하는 여정이다.** 각 챕터는 이전 챕터의 한계를 해결하며 한 단계씩 성장합니다.

이제 직접 Docker를 배워 보겠습니다. 챕터 2에서는 Docker가 컨테이너를 어떻게 만드는지 원리를 이해하고, 설치부터 이미지 생성까지 직접 따라 합니다.
